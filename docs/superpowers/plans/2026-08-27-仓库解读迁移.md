# 觅码仓库解读迁移 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地《2026-08-27-觅码-仓库解读-design.md》：把 Concept_Simplifier（`C:\Users\aefar\source\repos\Concept_Simplifier`，只读）的概念发散图迁移进觅码仓库页「仓库解读」tab——后端 FastAPI 落入本仓库 `backend/`（端口 8100，新增 GitHub 仓库理解与 `/api/repos/root`），前端落入 `frontend/src/explain/`（进入 tab 自动以仓库为根建图），功能全保留、节点色不变、白底米灰连线、去页面装饰。

**Architecture:** 后端逐字复制原型 `backend/app` 后做三处扩展（`gh.py` 仓库理解包、会话携带 `repo_context` 注入各 prompt、`/api/repos/root` 一次性返回根+首层）；前端逐字复制 `graph/` 引擎与 `store/` 与组件，样式经 `.explain-root` 作用域令牌重映射换肤（组件零改动），新增 `ExplainPanel` 组装进 `RepoPage` 的 explain tab，Vite 代理 `/explain-api` → `:8100`。

**Tech Stack:** 后端 Python + FastAPI + LangGraph + httpx + pytest；前端 React 18 + zustand + d3-force/d3-drag/d3-selection/d3-zoom（新依赖，仅此四个）+ vitest。

## Global Constraints

- **绝对禁止修改** `C:\Users\aefar\source\repos\Concept_Simplifier` 内任何文件——只读复制；一切适配都在本仓库副本上进行。
- 觅码工作区存在他人未提交改动（FeaturedHero 等）——每次提交只 `git add` 本任务 **Files** 清单中的路径，禁止 `git add -A` / `git add .`。
- 裸色值只允许出现在 `src/styles/theme.css` 与 `src/explain/explain-tokens.css`（后者是本功能登记的唯一例外，见 spec §5.3）。
- explain 相关样式一律收在 `.explain-root` 作用域内；界面文案简体中文。
- 节点色逐字不变：`#D08159 / #E7B88A / #E3C77F / #F0DDBE / #4A3F33 / #8A7B68`；连线用 `var(--line_regular)`；画布底 `var(--bg1)`。
- 后端会话仅内存（与原型一致）；解读后端不接觅码数据库；`LLM_MOCK=true` 时全链路离线（不触 GitHub、不调真实 LLM）。
- Run 命令 workdir：后端命令在 `backend`，前端命令在 `frontend`。提交信息风格沿用 `feat(backend): …` / `feat(frontend): …`。
- 复制一律用 PowerShell `Copy-Item`（保持内容逐字节一致），复制后只做任务中列明的修改。

---

### Task 1: 后端骨架迁移（逐字复制 + pytest 绿）

**Files:**
- Create: `backend/requirements.txt`、`backend/.env.example`
- Create: `backend/app/__init__.py`、`backend/app/config.py`、`backend/app/llm.py`、`backend/app/schemas.py`、`backend/app/main.py`
- Create: `backend/app/agent/__init__.py`、`backend/app/agent/graph.py`、`backend/app/agent/mock.py`、`backend/app/agent/prompts.py`、`backend/app/agent/websearch.py`
- Create: `backend/tests/__init__.py`、`backend/tests/conftest.py`、`backend/tests/test_detail.py`、`backend/tests/test_llm_override.py`、`backend/tests/test_reader_chat.py`

**Interfaces:**
- Produces: 原型 API 全集（`/api/health`、`/api/sessions`、`/api/roots`、`/api/expand`、`/api/nodes/detail`、`/api/reader/chat`），后续 Task 3 在此之上加 `/api/repos/root`。

- [ ] **Step 1: 逐字复制后端源码**

```powershell
New-Item -ItemType Directory -Force backend\app\agent, backend\tests | Out-Null
$src = "C:\Users\aefar\source\repos\Concept_Simplifier\backend"
Copy-Item "$src\requirements.txt" backend\
Copy-Item "$src\app\__init__.py","$src\app\config.py","$src\app\llm.py","$src\app\schemas.py","$src\app\main.py" backend\app\
Copy-Item "$src\app\agent\__init__.py","$src\app\agent\graph.py","$src\app\agent\mock.py","$src\app\agent\prompts.py","$src\app\agent\websearch.py" backend\app\agent\
Copy-Item "$src\tests\__init__.py","$src\tests\conftest.py","$src\tests\test_detail.py","$src\tests\test_llm_override.py","$src\tests\test_reader_chat.py" backend\tests\
```

- [ ] **Step 2: 写 `backend/.env.example`**

```bash
# LLM(OpenAI 兼容);LLM_MOCK=true 时全部可空
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=
LLM_MODEL=deepseek-chat
LLM_TEMPERATURE=0.7
LLM_MOCK=true

# 阅读器伴读联网搜索(可空,也可由前端设置区按请求提供)
TAVILY_API_KEY=

# GitHub 只读 API(匿名限额 60 次/小时;生产必配)
GITHUB_TOKEN=
```

- [ ] **Step 3: 安装依赖并运行原型测试**

Run(workdir: backend): `python -m pip install -r requirements.txt`
Run(workdir: backend): `python -m pytest`
Expected: PASS（原型的 detail / llm_override / reader_chat 用例全绿；conftest 已处理 mock。若有用例因缺 `.env` 失败，在 `backend/.env` 写入 `LLM_MOCK=true` 后重跑）

- [ ] **Step 4: Commit**

```bash
git add backend
git commit -m "feat(backend): 迁移 Concept_Simplifier FastAPI 服务骨架（agent 工作流/阅读器/测试）"
```

---

### Task 2: GitHub 仓库理解包 gh.py

**Files:**
- Create: `backend/app/gh.py`
- Modify: `backend/app/config.py`（末尾追加一行）
- Test: `backend/tests/test_gh.py`

**Interfaces:**
- Produces: `gh.fetch_repo_context(full_name: str, default_branch: str | None = None, transport: httpx.BaseTransport | None = None) -> dict`（键：`full_name/default_branch/description/readme/tree_text/languages_text`）；`gh.GitHubFetchError(RuntimeError)`。Task 3 的 `main.py` 与测试依赖这两个名字。

- [ ] **Step 1: 写失败测试 `backend/tests/test_gh.py`**

```python
"""gh.fetch_repo_context:GitHub 拉取与降级(MockTransport,离线可跑)。"""
import httpx
import pytest

from app import config, gh


def _handler(request: httpx.Request) -> httpx.Response:
    path = request.url.path
    if path == "/repos/octocat/demo":
        return httpx.Response(200, json={"default_branch": "main", "description": "demo repo"})
    if path == "/repos/octocat/demo/readme":
        return httpx.Response(200, text="# Demo\nhello world")
    if path == "/repos/octocat/demo/git/trees/main":
        return httpx.Response(200, json={"tree": [
            {"type": "blob", "path": "README.md"},
            {"type": "tree", "path": "src"},
            {"type": "blob", "path": "src/main.py"},
            {"type": "blob", "path": "src/agent.py"},
        ]})
    if path == "/repos/octocat/demo/languages":
        return httpx.Response(200, json={"Python": 8000, "TypeScript": 2000})
    return httpx.Response(404)


def test_fetch_repo_context_assembles_package():
    repo = gh.fetch_repo_context("octocat/demo", transport=httpx.MockTransport(_handler))
    assert repo["full_name"] == "octocat/demo"
    assert repo["default_branch"] == "main"
    assert repo["description"] == "demo repo"
    assert repo["readme"].startswith("# Demo")
    assert "src/" in repo["tree_text"]
    assert "Python" in repo["languages_text"] and "80%" in repo["languages_text"]


def test_fetch_repo_context_missing_repo():
    with pytest.raises(gh.GitHubFetchError, match="仓库不存在"):
        gh.fetch_repo_context("octocat/ghost", transport=httpx.MockTransport(_handler))


def test_fetch_repo_context_rate_limited(monkeypatch):
    monkeypatch.setattr(config, "GITHUB_TOKEN", "")
    limited = httpx.MockTransport(lambda req: httpx.Response(403))
    with pytest.raises(gh.GitHubFetchError, match="限流"):
        gh.fetch_repo_context("octocat/demo", transport=limited)
```

- [ ] **Step 2: 运行确认失败**

Run(workdir: backend): `python -m pytest tests/test_gh.py -v`
Expected: FAIL —— `ImportError: cannot import name 'gh'`

- [ ] **Step 3: 实现 `backend/app/gh.py`**

```python
"""GitHub 仓库理解包拉取:仅官方只读 API(元信息/README/文件树/语言统计)。
觅码仓库解读专用;失败一律抛 GitHubFetchError(message 面向用户直接展示)。"""
from __future__ import annotations

import httpx

from . import config

GITHUB_API = "https://api.github.com"
README_MAX_CHARS = 4000
TREE_MAX_LINES = 60
TIMEOUT_SECONDS = 10.0


class GitHubFetchError(RuntimeError):
    """GitHub 拉取失败;message 直接展示给用户。"""


def _headers(token: str) -> dict[str, str]:
    headers = {
        "Accept": "application/vnd.github+json",
        "X-GitHub-Api-Version": "2022-11-28",
        "User-Agent": "meecode-explain",
    }
    if token:
        headers["Authorization"] = f"Bearer {token}"
    return headers


def _truncate(text: str, limit: int) -> str:
    text = text.strip()
    if len(text) <= limit:
        return text
    return text[:limit] + "…(已截断)"


def _summarize_tree(entries: list[dict]) -> str:
    """文件树摘要:仅顶层文件与一级目录,超 TREE_MAX_LINES 行截断。"""
    lines: list[str] = []
    for entry in entries:
        path = str(entry.get("path", "")).strip("/")
        if not path or "/" in path:
            continue
        lines.append(f"{path}/" if entry.get("type") == "tree" else path)
    lines = sorted(set(lines))
    if len(lines) > TREE_MAX_LINES:
        lines = lines[:TREE_MAX_LINES] + [f"…(已截断,共 {len(lines)} 项)"]
    return "\n".join(lines)


def _summarize_languages(languages: dict[str, int]) -> str:
    total = sum(languages.values())
    if total <= 0:
        return ""
    ranked = sorted(languages.items(), key=lambda kv: kv[1], reverse=True)
    return "、".join(f"{name} {round(count * 100 / total)}%" for name, count in ranked)


def fetch_repo_context(
    full_name: str,
    default_branch: str | None = None,
    transport: httpx.BaseTransport | None = None,
) -> dict:
    """拉取仓库理解包;transport 参数仅供测试注入(httpx.MockTransport)。"""
    full_name = full_name.strip().strip("/")
    headers = _headers(config.GITHUB_TOKEN)
    try:
        with httpx.Client(
            base_url=GITHUB_API,
            headers=headers,
            timeout=TIMEOUT_SECONDS,
            follow_redirects=True,
            transport=transport,
        ) as client:
            meta_resp = client.get(f"/repos/{full_name}")
            if meta_resp.status_code == 404:
                raise GitHubFetchError("GitHub 仓库不存在或已私有")
            if meta_resp.status_code == 403:
                raise GitHubFetchError("GitHub API 限流:请稍后重试,或在 backend/.env 配置 GITHUB_TOKEN")
            meta_resp.raise_for_status()
            meta = meta_resp.json()
            branch = default_branch or meta.get("default_branch") or "HEAD"

            readme = ""
            readme_resp = client.get(
                f"/repos/{full_name}/readme",
                headers={**headers, "Accept": "application/vnd.github.raw"},
            )
            if readme_resp.status_code == 200:
                readme = _truncate(readme_resp.text, README_MAX_CHARS)

            tree_text = ""
            tree_resp = client.get(f"/repos/{full_name}/git/trees/{branch}", params={"recursive": "1"})
            if tree_resp.status_code == 200:
                tree_text = _summarize_tree(tree_resp.json().get("tree", []))

            languages_text = ""
            lang_resp = client.get(f"/repos/{full_name}/languages")
            if lang_resp.status_code == 200:
                languages_text = _summarize_languages(lang_resp.json())
    except httpx.HTTPError as exc:
        raise GitHubFetchError(f"GitHub 连接失败:{exc}") from exc

    return {
        "full_name": full_name,
        "default_branch": branch,
        "description": meta.get("description") or "",
        "readme": readme,
        "tree_text": tree_text,
        "languages_text": languages_text,
    }
```

- [ ] **Step 4: `backend/app/config.py` 末尾追加（`TAVILY_API_KEY` 行之后）**

```python

GITHUB_TOKEN: str = os.getenv("GITHUB_TOKEN", "")
```

- [ ] **Step 5: 运行测试确认通过**

Run(workdir: backend): `python -m pytest tests/test_gh.py -v` 然后 `python -m pytest`
Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/gh.py backend/app/config.py backend/tests/test_gh.py
git commit -m "feat(backend): GitHub 仓库理解包拉取（README/文件树/语言，GITHUB_TOKEN 可选）"
```

---

### Task 3: 仓库上下文注入 + /api/repos/root

**Files:**
- Modify: `backend/app/agent/prompts.py`（末尾追加）
- Modify: `backend/app/agent/graph.py`（5 处，见下）
- Modify: `backend/app/agent/mock.py`（末尾追加）
- Modify: `backend/app/schemas.py`（末尾追加）
- Modify: `backend/app/main.py`（import、会话、新端点、3 个既有端点透传）
- Test: `backend/tests/test_repo_root.py`

**Interfaces:**
- Produces: `POST /api/repos/root`，请求 `{session_id, full_name, default_branch?}`，响应 `{node: NodePayload, children: NodePayload[], edges: EdgePayload[]}`；`run_expand(..., repo_context: dict | None)`、`run_elaborate(..., repo_context: dict | None)`、`run_repo_topic(full_name, description, readme, llm)`。前端 Task 5/7 依赖响应结构。

- [ ] **Step 1: 写失败测试 `backend/tests/test_repo_root.py`**

```python
"""/api/repos/root:mock 模式端到端(不触 GitHub、不调真实 LLM)。"""
import pytest
from fastapi.testclient import TestClient

from app import config
from app.main import app

client = TestClient(app)


@pytest.fixture(autouse=True)
def _mock_llm(monkeypatch):
    monkeypatch.setattr(config, "LLM_MOCK", True)


def _make_session() -> str:
    resp = client.post("/api/sessions")
    assert resp.status_code == 200
    return resp.json()["session_id"]


def test_repo_root_returns_root_children_edges():
    session_id = _make_session()
    resp = client.post(
        "/api/repos/root",
        json={"session_id": session_id, "full_name": "octocat/demo"},
    )
    assert resp.status_code == 200
    body = resp.json()
    assert body["node"]["title"] == "octocat/demo 仓库的解读"
    assert len(body["children"]) >= 1
    assert len(body["edges"]) == len(body["children"])
    for edge in body["edges"]:
        assert edge["parent_id"] == body["node"]["id"]


def test_repo_root_requires_session():
    resp = client.post("/api/repos/root", json={"session_id": "nope", "full_name": "octocat/demo"})
    assert resp.status_code == 404


def test_repo_context_feeds_expand_in_same_session():
    session_id = _make_session()
    root = client.post(
        "/api/repos/root", json={"session_id": session_id, "full_name": "octocat/demo"}
    ).json()
    parent = root["children"][0]
    resp = client.post(
        "/api/expand",
        json={
            "session_id": session_id,
            "node_id": parent["id"],
            "node_title": parent["title"],
            "path": [root["node"]["title"], parent["title"]],
            "depth": 1,
        },
    )
    assert resp.status_code == 200
    assert resp.json()["refused"] is None
```

- [ ] **Step 2: 运行确认失败**

Run(workdir: backend): `python -m pytest tests/test_repo_root.py -v`
Expected: FAIL —— 404 Not Found（`/api/repos/root` 不存在）

- [ ] **Step 3: `prompts.py` 末尾追加**

```python

# ---------- 仓库解读上下文(觅码迁移新增) ----------

REPO_CONTEXT_BLOCK = """

【仓库上下文】
这张概念图是对 GitHub 仓库「{full_name}」的解读。
仓库简介:{description}
主要语言:{languages_text}
目录结构概览:
{tree_text}
README 摘录:
{readme}

要求:结合以上仓库实情解释概念——子节点/阐述应指向该仓库实际使用的模块、技术与设计;不要脱离仓库空谈通用知识。
"""


def repo_block(repo: dict | None) -> str:
    """repo_context 存在时返回追加块,否则空串(通用概念图行为不变)。"""
    if not repo:
        return ""
    return REPO_CONTEXT_BLOCK.format(
        full_name=repo.get("full_name", ""),
        description=repo.get("description") or "(无)",
        languages_text=repo.get("languages_text") or "(未知)",
        tree_text=repo.get("tree_text") or "(无)",
        readme=repo.get("readme") or "(无)",
    )


REPO_TOPIC_SYSTEM = """你是仓库解读助手。给定一个 GitHub 仓库的简介与 README 摘录,输出一个简短的名词性陈述短语作为解读图的中心节点标题,形如「XX 仓库的解读」。

规则:
1. 以主题为核心的名词性陈述,不是疑问句;
2. 不超过 16 个字;
3. 只输出 JSON:{"topic": "..."}。
"""

REPO_TOPIC_USER = """仓库:{full_name}
简介:{description}
README 摘录:
{readme}
"""
```

- [ ] **Step 4: `mock.py` 末尾追加**

```python


def mock_repo_context(full_name: str) -> dict:
    """mock 模式的仓库理解包:离线开发/联调用,不触 GitHub。"""
    return {
        "full_name": full_name,
        "default_branch": "main",
        "description": "演示用仓库:离线 mock 模式",
        "readme": "# Demo\n这是一个演示仓库,用于离线开发与前端联调。",
        "tree_text": "README.md\nsrc/\n  main.py\n  agent.py",
        "languages_text": "Python 100%",
    }
```

- [ ] **Step 5: `graph.py` 五处修改**

1. `AgentState` 中 `refused: Optional[str]` 之后加一行：

```python
    repo_context: Optional[dict]
```

2. `decompose_node` 中 `chat_json(...)` 的 user 参数改为（format 后追加仓库块）：

```python
    result = chat_json(
        prompts.DECOMPOSE_SYSTEM,
        prompts.DECOMPOSE_USER.format(
            parent_title=state["parent_title"],
            path=" → ".join(state["path"]) or state["parent_title"],
            max_children=settings.max_children,
        )
        + prompts.repo_block(state.get("repo_context")),
        DecomposeResult,
        llm=state.get("llm"),
    )
```

3. `relate_node` 同样追加：

```python
    result = chat_json(
        prompts.RELATE_SYSTEM,
        prompts.RELATE_USER.format(
            parent_title=state["parent_title"],
            path=" → ".join(state["path"]) or state["parent_title"],
            children_titles=titles,
        )
        + prompts.repo_block(state.get("repo_context")),
        RelateResult,
        llm=state.get("llm"),
    )
```

4. `run_expand` 换签名并在 invoke 时透传：

```python
def run_expand(
    parent_title: str,
    path: list[str],
    depth: int,
    settings: Settings,
    llm: LLMOverride | None = None,
    repo_context: Optional[dict] = None,
) -> tuple[list[ChildSpec], list[EdgeDescription], Optional[str]]:
    state = _expand_graph.invoke(
        {
            "parent_title": parent_title,
            "path": path,
            "depth": depth,
            "settings": settings,
            "llm": llm,
            "repo_context": repo_context,
        }
    )
    return state.get("children") or [], state.get("edges") or [], state.get("refused")
```

5. `run_elaborate` 换签名并追加仓库块，其后新增 `run_repo_topic`：

```python
def run_elaborate(
    node_title: str,
    path: list[str],
    brief: str,
    llm: LLMOverride | None = None,
    repo_context: Optional[dict] = None,
) -> str:
    """详细展开:为已展开节点生成更丰富的 markdown 阐述(不产生新子节点)。"""
    if config.LLM_MOCK:
        return mock.mock_elaborate(node_title, path, brief)
    result = chat_json(
        prompts.ELABORATE_SYSTEM,
        prompts.ELABORATE_USER.format(
            node_title=node_title,
            path=" → ".join(path) or node_title,
            brief=brief or node_title,
        )
        + prompts.repo_block(repo_context),
        ElaborateResult,
        llm=llm,
    )
    return result.detail.strip()


def run_repo_topic(
    full_name: str, description: str, readme: str, llm: LLMOverride | None = None
) -> str:
    """仓库解读根主题:按仓库实情生成"XX 的解读"式名词性陈述。"""
    if config.LLM_MOCK:
        return f"{full_name} 仓库的解读"
    result = chat_json(
        prompts.REPO_TOPIC_SYSTEM,
        prompts.REPO_TOPIC_USER.format(
            full_name=full_name, description=description or "(无)", readme=readme or "(无)"
        ),
        RewriteResult,
        llm=llm,
    )
    return result.topic.strip()
```

- [ ] **Step 6: `schemas.py` 末尾追加**

```python

class RepoRootRequest(BaseModel):
    """以仓库为根建图(觅码仓库解读)。"""

    session_id: str
    full_name: str = Field(min_length=1, max_length=200, description="owner/repo")
    default_branch: Optional[str] = None
    llm: Optional[LLMOverride] = None


class RepoRootResponse(BaseModel):
    """根节点 + 首层子节点 + 双向边(后端已替前端完成首层展开)。"""

    node: NodePayload
    children: list[NodePayload] = Field(default_factory=list)
    edges: list[EdgePayload] = Field(default_factory=list)
```

- [ ] **Step 7: `main.py` 修改**

1. import 区改为（保留原型 `mock_chat_events` 的直接导入，供 reader_chat mock 分支继续使用）：

```python
from . import config, gh
from .agent import prompts
from .agent.graph import run_elaborate, run_expand, run_repo_topic, run_rewrite
from .agent.mock import mock_chat_events, mock_repo_context
from .llm import chat_stream_events
from .schemas import (
    ChatRequest,
    CreateRootRequest,
    CreateRootResponse,
    CreateSessionResponse,
    DetailRequest,
    DetailResponse,
    EdgePayload,
    ExpandRequest,
    ExpandResponse,
    NodePayload,
    RepoRootRequest,
    RepoRootResponse,
    Settings,
)
```

2. `create_session` 中会话字典改为：

```python
    _sessions[session_id] = {"created_at": time.time(), "repo_context": None}
```

3. 在 `create_root` 之后新增端点：

```python
@app.post("/api/repos/root", response_model=RepoRootResponse)
def create_repo_root(req: RepoRootRequest) -> RepoRootResponse:
    """以仓库为根建图:拉取理解包 → 主题陈述 → 自动首层展开。"""
    _require_session(req.session_id)
    if config.LLM_MOCK:
        repo = mock_repo_context(req.full_name.strip().strip("/"))
    else:
        try:
            repo = gh.fetch_repo_context(req.full_name, req.default_branch)
        except gh.GitHubFetchError as exc:
            raise HTTPException(status_code=502, detail=str(exc)) from exc
    _sessions[req.session_id]["repo_context"] = repo

    try:
        topic = run_repo_topic(repo["full_name"], repo["description"], repo["readme"], llm=req.llm)
        children, edges, _refused = run_expand(
            parent_title=topic,
            path=[topic],
            depth=0,
            settings=Settings(),
            llm=req.llm,
            repo_context=repo,
        )
    except RuntimeError as exc:
        raise HTTPException(status_code=502, detail=str(exc)) from exc

    node = NodePayload(id=uuid.uuid4().hex, title=topic, content=topic, relevance=1.0)
    edge_by_title = {e.child_title: e for e in edges}
    child_payloads = [
        NodePayload(
            id=uuid.uuid4().hex,
            title=c.title,
            content=c.content,
            node_type=c.node_type,
            relevance=c.relevance,
        )
        for c in children
    ]
    edge_payloads = [
        EdgePayload(
            id=uuid.uuid4().hex,
            parent_id=node.id,
            child_id=p.id,
            forward=edge_by_title[p.title].forward if p.title in edge_by_title else "",
            backward=edge_by_title[p.title].backward if p.title in edge_by_title else "",
        )
        for p in child_payloads
    ]
    return RepoRootResponse(node=node, children=child_payloads, edges=edge_payloads)
```

4. `expand` 端点：`run_expand(...)` 调用加一行参数（`llm=req.llm,` 之后）：

```python
            repo_context=_sessions[req.session_id].get("repo_context"),
```

5. `node_detail` 端点：`run_elaborate(...)` 调用加同一行参数。

6. `reader_chat` 端点：`context = (...)` 赋值之后追加一行：

```python
        context += prompts.repo_block(_sessions[req.session_id].get("repo_context"))
```

- [ ] **Step 8: 运行测试确认通过**

Run(workdir: backend): `python -m pytest`
Expected: 全部 PASS（含原 detail/llm_override/reader_chat 既有用例）

- [ ] **Step 9: Commit**

```bash
git add backend/app/agent/prompts.py backend/app/agent/graph.py backend/app/agent/mock.py backend/app/schemas.py backend/app/main.py backend/tests/test_repo_root.py
git commit -m "feat(backend): /api/repos/root 仓库根建图与会话级仓库上下文注入"
```

---

### Task 4: 后端 mock 冒烟验证

**Files:** 无新增（验证任务）

- [ ] **Step 1: 启动 mock 服务**

Run(workdir: backend): `$env:LLM_MOCK='true'; python -m uvicorn app.main:app --port 8100`
（前台阻塞，保持运行）

- [ ] **Step 2: 另一终端验证三连**

```powershell
curl.exe http://127.0.0.1:8100/api/health
$sid = (curl.exe -s -X POST http://127.0.0.1:8100/api/sessions | ConvertFrom-Json).session_id
curl.exe -s -X POST http://127.0.0.1:8100/api/repos/root -H "Content-Type: application/json" -d "{\"session_id\":\"$sid\",\"full_name\":\"octocat/Hello-World\"}"
```

Expected: health 返回 `{"ok":true,...}`；repos/root 返回含 `node`（标题 `octocat/Hello-World 仓库的解读`）、非空 `children` 与 `edges` 的 JSON。离线可完成 = mock 全链路贯通。

- [ ] **Step 3: 关闭服务**（无提交）

---

### Task 5: 前端依赖 + 引擎/状态/API 迁移

**Files:**
- Modify: `frontend/package.json`（新增 d3 四件套）
- Create: `frontend/src/explain/api/client.ts`（复制+改）、`frontend/src/explain/graph/{controller,inertia,layout,simulation,sizing,zoomFit}.ts`、`frontend/src/explain/store/{graphStore,readerStore,sessionStore,settingsStore,uiStore}.ts`
- Test: `frontend/src/explain/api/chatStream.test.ts`、`frontend/src/explain/graph/{inertia,layout,sizing,zoomFit,controller}.test.ts`、`frontend/src/explain/store/{settingsStore,readerStore}.test.ts`（复制自原型 `frontend/tests/`，路径重写见 Step 3）

**Interfaces:**
- Consumes: 原型 `frontend/src/{graph,store,api}` 全部源文件（只读复制）。
- Produces: `src/explain/api/client.ts` 导出 `api`（含新方法 `repoRoot`）与 `ApiError`；`useGraphStore` 的 `setRoot/addChildren`、`useSessionStore` 的 `ensureSession/enterPractice/resetSession` 等，Task 7 的 ExplainPanel 直接调用。

- [ ] **Step 1: 安装 d3 依赖**

Run(workdir: frontend): `npm install d3-force@^3.0.0 d3-drag@^3.0.0 d3-selection@^3.0.0 d3-zoom@^3.0.0; npm install -D @types/d3-force @types/d3-drag @types/d3-selection @types/d3-zoom`
Expected: package.json dependencies 出现四包，devDependencies 出现四类型包。

- [ ] **Step 2: 逐字复制源文件**

```powershell
New-Item -ItemType Directory -Force frontend\src\explain\api, frontend\src\explain\graph, frontend\src\explain\store | Out-Null
$fsrc = "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\src"
Copy-Item "$fsrc\graph\controller.ts","$fsrc\graph\inertia.ts","$fsrc\graph\layout.ts","$fsrc\graph\simulation.ts","$fsrc\graph\sizing.ts","$fsrc\graph\zoomFit.ts" frontend\src\explain\graph\
Copy-Item "$fsrc\store\graphStore.ts","$fsrc\store\readerStore.ts","$fsrc\store\sessionStore.ts","$fsrc\store\settingsStore.ts","$fsrc\store\uiStore.ts" frontend\src\explain\store\
Copy-Item "$fsrc\api\client.ts" frontend\src\explain\api\
```

- [ ] **Step 3: 迁移单测并重写 import 路径**

```powershell
Copy-Item "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\chatStream.test.ts" frontend\src\explain\api\
Copy-Item "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\inertia.test.ts","C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\layout.test.ts","C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\sizing.test.ts","C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\zoomFit.test.ts" frontend\src\explain\graph\
Copy-Item "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\settingsStore.test.ts","C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\readerStore.test.ts" frontend\src\explain\store\
Copy-Item "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\tests\detailFlow.test.ts" frontend\src\explain\graph\controller.test.ts
```

按对照表重写每个测试文件的 import / `vi.mock` 路径：

| 文件（新位置） | 原字符串 → 新字符串 |
|---|---|
| `api/chatStream.test.ts` | `"../src/api/client"` → `"./client"` |
| `graph/inertia.test.ts`、`layout`、`sizing`、`zoomFit` | `"../src/graph/xxx"` → `"./xxx"` |
| `store/settingsStore.test.ts`、`readerStore.test.ts` | `"../src/store/xxx"` → `"./xxx"`（`vi.mock("../src/api/client")` → `"../api/client"`） |
| `graph/controller.test.ts`（原 detailFlow） | `"../src/api/client"` → `"../api/client"`（仅去掉 `../src` 前缀）；`"../src/graph/controller"` → `"./controller"`；`"../src/store/graphStore"` → `"../store/graphStore"`；`"../src/store/readerStore"` → `"../store/readerStore"`；`"../src/store/sessionStore"` → `"../store/sessionStore"`；**并整段删除文件顶部 localStorage/window polyfill（原 4–24 行）**——觅码 vitest 是 jsdom 环境，自带两者 |

- [ ] **Step 4: `explain/api/client.ts` 两处修改**

1. `request<T>` 内 `fetch(\`/api${path}\`` 改为 `fetch(\`/explain-api${path}\``；`chatStream` 内 `fetch("/api/reader/chat"` 改为 `fetch("/explain-api/reader/chat"`。
2. `api` 对象中 `createRoot` 之后新增方法：

```ts
  repoRoot: (
    sessionId: string,
    payload: { full_name: string; default_branch?: string | null; llm?: LLMOverrideDto },
  ) =>
    request<{ node: NodePayload; children: NodePayload[]; edges: EdgePayload[] }>("/repos/root", {
      method: "POST",
      body: JSON.stringify({ session_id: sessionId, ...payload }),
    }),
```

- [ ] **Step 5: `explain/store/settingsStore.ts` persist 键改名**

`name: "cs-model-settings"` → `name: "meecode-explain-model-settings"`（注释同步为「觅码仓库解读设置」）。

- [ ] **Step 6: 运行迁移的单测**

Run(workdir: frontend): `npx vitest run src/explain`
Expected: 全部 PASS（node 逻辑测试在 jsdom 下行为一致；controller.test.ts 不再需要 polyfill）

- [ ] **Step 7: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/explain
git commit -m "feat(frontend): 迁移概念图引擎/状态层/API 客户端至 explain 命名空间（/explain-api 前缀 + repoRoot）"
```

---

### Task 6: 组件迁移 + 令牌与样式改造

**Files:**
- Create: `frontend/src/explain/components/{GraphCanvas,NodeView,NodeCard,EdgeView,EdgeBubble,Toasts,PracticeBar,SettingsPanel}.tsx`、`frontend/src/explain/components/reader/{ReaderPanel,ReaderArticle,ReaderThread,ReaderComposer}.tsx`（复制）
- Create: `frontend/src/explain/explain-tokens.css`、`frontend/src/explain/explain.css`

**Interfaces:**
- Consumes: Task 5 的 store/api/graph 模块。
- Produces: `<GraphCanvas/>`、`<SettingsPanel/>`、`<ReaderPanel/>`、`<PracticeBar/>`、`<Toasts/>`、`openReaderFromTopbar`（controller 内）——Task 7 的 ExplainPanel 直接使用；`.explain-root` 作用域令牌。

- [ ] **Step 1: 复制组件并修正 reader 层级**

```powershell
New-Item -ItemType Directory -Force frontend\src\explain\components\reader | Out-Null
$csrc = "C:\Users\aefar\source\repos\Concept_Simplifier\frontend\src\components"
Copy-Item "$csrc\GraphCanvas.tsx","$csrc\NodeView.tsx","$csrc\NodeCard.tsx","$csrc\EdgeView.tsx","$csrc\EdgeBubble.tsx","$csrc\Toasts.tsx","$csrc\PracticeBar.tsx","$csrc\SettingsPanel.tsx" frontend\src\explain\components\
Copy-Item "$csrc\reader\ReaderPanel.tsx","$csrc\reader\ReaderArticle.tsx","$csrc\reader\ReaderThread.tsx","$csrc\reader\ReaderComposer.tsx" frontend\src\explain\components\reader\
```

原型 `components/reader/*` 相对 `../store`、`../api` 的层级比新位置浅一级，统一加一级前缀：

```powershell
Get-ChildItem frontend\src\explain\components\reader\*.tsx | ForEach-Object {
  (Get-Content $_.FullName -Raw) -replace '"\.\./', '"../../' | Set-Content $_.FullName -NoNewline
}
```

（reader 内部 `"./Xxx"` 相对导入不受影响；`components/*.tsx` 的 `"../store|../graph|../api"` 层级恰好一致，无需改。）

- [ ] **Step 2: 写 `frontend/src/explain/explain-tokens.css`**

在 `.explain-root` 作用域内**沿用原型令牌名、只改裁决过的值**（组件与样式零改名即换肤）：

```css
/* src/explain/explain-tokens.css —— 仓库解读板块局部令牌。
   节点色逐字取自原型 docs/design-system.md(迁移决策:节点颜色不变);
   本文件是 theme.css 之外唯一允许裸色值的出处(spec §5.3)。 */
.explain-root {
  /* 色板:节点色不变;面层/底色换觅码 token;连线米灰(spec 决策 7) */
  --rice-paper: var(--bg1);
  --sand: var(--bg1);
  --sand-deep: var(--bg2);
  --ink-brown: #4a3f33;
  --ink-soft: #8a7b68;
  --terracotta: #d08159;
  --terracotta-deep: #b96a45;
  --apricot: #e7b88a;
  --wheat: #e3c77f;
  --cream-node: #f0ddbe;
  --bush: #9caf88;
  --bush-deep: #7c9170;
  --edge-white: var(--line_regular);
  /* 字体:并入觅码栈 */
  --font-display: var(--font-body);
  --font-ui: var(--font-body);
  /* 字阶/圆角/阴影/动效:保留原型画布内节奏(节点素材的一部分) */
  --fs-hero: 56px;
  --fs-page-title: 32px;
  --fs-card-title: 18px;
  --fs-ui: 13px;
  --fs-caption: 11.5px;
  --radius-pill: 999px;
  --radius-card: 20px;
  --radius-bubble: 16px;
  --shadow-soft: var(--shadow_dropdown);
  --shadow-float: var(--shadow_float);
  --dur-node-spawn: 450ms;
  --dur-radius: 400ms;
  --dur-bubble: 180ms;
  --dur-page: 300ms;
  --ease-soft: cubic-bezier(0.22, 1, 0.36, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  /* 品牌色上的文字(主按钮白字) */
  --ex-on-accent: #fffdf8;
}
```

- [ ] **Step 3: 生成 `frontend/src/explain/explain.css`**

1. 复制 `C:\Users\aefar\source\repos\Concept_Simplifier\frontend\src\styles\app.css` → `frontend/src/explain/explain.css`；
2. 每个顶层选择器加前缀 `.explain-root `（`html`/`body` 规则整段删除）；
3. 整段删除装饰：四角灌木（`.corner-bush*`）、八边形框（`.workspace-frame*` 等）、输入阶段（`.input-stage`、`.concept-input-box*`、`.input-hint`、`.sprout-empty`）、签名形变（`.morph-clone*`）、介绍页专属规则；
4. `.reader-panel`、`.settings-panel`、`.toasts` 内的 `position: fixed` 改 `position: absolute`；
5. 文件末尾追加觅码容器/工具栏/三态样式：

```css
/* ---- 觅码容器与三态(spec §5.2/§5.4) ---- */
.explain-root {
  position: relative;
  height: min(640px, 72vh);
  overflow: hidden;
  background: var(--bg1);
  border: 1px solid var(--line_light);
  border-radius: var(--radius-xl);
}
.explain-root .graph-stage { position: absolute; inset: 0; }

.explain-toolbar {
  position: absolute;
  top: 12px;
  right: 12px;
  z-index: 30;
  display: flex;
  gap: 8px;
}
.explain-tool-btn {
  height: 32px;
  padding: 0 12px;
  display: inline-flex;
  align-items: center;
  gap: 4px;
  background: var(--bg1);
  color: var(--text1);
  border: 1px solid var(--line_regular);
  border-radius: var(--radius-md);
  font-size: 13px;
  cursor: pointer;
  transition: background-color var(--time-fast) var(--ease), color var(--time-fast) var(--ease);
}
.explain-tool-btn:hover:not(:disabled) { background: var(--brand_blue_thin); }
.explain-tool-btn:disabled { color: var(--text4); cursor: not-allowed; }
.explain-tool-btn.active { color: var(--brand_blue); border-color: var(--brand_blue_line); background: var(--brand_blue_thin); }

.explain-loading, .explain-error {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 6px;
  text-align: center;
}
.explain-loading-title, .explain-error-title { font-size: 16px; font-weight: 500; color: var(--text2); }
.explain-loading-sub, .explain-error-sub { font-size: 13px; color: var(--text3); }
.explain-retry {
  margin-top: 8px;
  height: 36px;
  padding: 0 16px;
  background: var(--brand_blue);
  color: var(--ex-on-accent);
  border: none;
  border-radius: var(--radius-md);
  font-size: 14px;
  cursor: pointer;
}
.explain-retry:hover { background: var(--brand_blue_hover); }
```

6. 若后续 typecheck/运行时发现某 explain 组件类名在 explain.css 中缺规则，回原型 `styles/global.css` 找到对应段，按同样前缀规则补进 explain.css（组件不改动）。

- [ ] **Step 4: 类型检查**

Run(workdir: frontend): `npm run typecheck`
Expected: 0 error（若报某组件引用了未迁移模块，按报错把该依赖一并按复制规则迁入；禁止修改原仓库）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/explain/components frontend/src/explain/explain-tokens.css frontend/src/explain/explain.css
git commit -m "feat(frontend): 迁移解读组件族并接入 .explain-root 令牌（节点色不变/白底/米灰连线）"
```

---

### Task 7: ExplainPanel + RepoPage 接入 + 代理 + 页面测试

**Files:**
- Create: `frontend/src/explain/ExplainPanel.tsx`
- Modify: `frontend/src/pages/RepoPage.tsx`、`frontend/src/pages/RepoPage.css`、`frontend/vite.config.ts`
- Test: `frontend/src/pages/RepoPage.test.tsx`

**Interfaces:**
- Consumes: Task 5/6 全部导出；`RepoDetail`（`../api/types`，字段 `github_url`、`default_branch`）。
- Produces: `RepoPage` explain tab 内渲染 `<ExplainPanel repo={detail} />`；`?tab=explain` 深链语义不变。

- [ ] **Step 1: 写 `frontend/src/explain/ExplainPanel.tsx`**

```tsx
/** 仓库解读面板:进入 tab 即自动以仓库为根建图(spec §5.2)。
 * 编排自原型 WorkspacePage——去除输入阶段与页面装饰,图引擎/面板原样复用。 */
import { useCallback, useEffect, useState } from 'react'
import type { RepoDetail } from '../api/types'
import { api } from './api/client'
import { GraphCanvas } from './components/GraphCanvas'
import { PracticeBar } from './components/PracticeBar'
import { SettingsPanel } from './components/SettingsPanel'
import { Toasts } from './components/Toasts'
import { ReaderPanel } from './components/reader/ReaderPanel'
import { openReaderFromTopbar } from './graph/controller'
import { useGraphStore } from './store/graphStore'
import { useReaderStore } from './store/readerStore'
import { useSessionStore } from './store/sessionStore'
import { useUiStore } from './store/uiStore'
import './explain-tokens.css'
import './explain.css'

type Stage = 'loading' | 'graph' | 'error'

/** https://github.com/owner/repo(.git) → owner/repo */
export function repoFullName(githubUrl: string): string {
  const raw = githubUrl.trim()
  const path = raw
    .replace(/^https?:\/\/github\.com\//i, '')
    .replace(/\.git$/, '')
    .replace(/^\/+|\/+$/g, '')
  return path
}

interface Props {
  repo: RepoDetail
}

export default function ExplainPanel({ repo }: Props) {
  const [stage, setStage] = useState<Stage>('loading')
  const [errorMsg, setErrorMsg] = useState('')
  const full_name = repoFullName(repo.github_url)

  const rootId = useGraphStore((s) => s.rootId)
  const path = useSessionStore((s) => s.path)
  const mode = useSessionStore((s) => s.mode)
  const enterPractice = useSessionStore((s) => s.enterPractice)
  const resetSession = useSessionStore((s) => s.resetSession)
  const resetGraph = useGraphStore((s) => s.reset)
  const toggleSettings = useUiStore((s) => s.toggleSettings)
  const readerOpen = useReaderStore((s) => s.open)

  const build = useCallback(
    async (fullName: string, branch: string | null) => {
      setStage('loading')
      setErrorMsg('')
      useReaderStore.getState().reset()
      resetGraph()
      resetSession()
      try {
        const sessionId = await useSessionStore.getState().ensureSession()
        const result = await api.repoRoot(sessionId, { full_name: fullName, default_branch: branch })
        useGraphStore.getState().setRoot(result.node)
        if (result.children.length > 0) {
          useGraphStore.getState().addChildren(result.node.id, result.children, result.edges)
        }
        setStage('graph')
      } catch (err) {
        setErrorMsg(err instanceof Error ? err.message : '解读服务暂不可用')
        setStage('error')
      }
    },
    [resetGraph, resetSession],
  )

  useEffect(() => {
    void build(full_name, repo.default_branch || null)
  }, [build, full_name, repo.default_branch])

  return (
    <div className="explain-root">
      {stage === 'loading' && (
        <div className="explain-loading" role="status" aria-label="正在生成仓库解读">
          <p className="explain-loading-title">AI 正在阅读这个仓库…</p>
          <p className="explain-loading-sub">拉取 README 与目录结构，生成解读图</p>
        </div>
      )}
      {stage === 'error' && (
        <div className="explain-error" role="alert">
          <p className="explain-error-title">解读服务暂不可用</p>
          <p className="explain-error-sub">{errorMsg}</p>
          <button className="explain-retry" onClick={() => void build(full_name, repo.default_branch || null)}>
            重试
          </button>
        </div>
      )}
      {stage === 'graph' && rootId && (
        <>
          <div className="explain-toolbar">
            {mode === 'explore' && (
              <button
                className="explain-tool-btn"
                onClick={enterPractice}
                disabled={path.length === 0}
                title={path.length === 0 ? '先展开几个节点，再来练习' : '沿着你的探索轨迹回忆'}
              >
                练习模式
              </button>
            )}
            <button className="explain-tool-btn" onClick={toggleSettings} aria-label="设置">
              <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round">
                <circle cx="12" cy="12" r="3.2" />
                <path d="M12 2.8v3M12 18.2v3M21.2 12h-3M5.8 12h-3M18.5 5.5l-2.1 2.1M7.6 16.4l-2.1 2.1M18.5 18.5l-2.1-2.1M7.6 7.6L5.5 5.5" />
              </svg>
            </button>
            <button
              className={`explain-tool-btn${readerOpen ? ' active' : ''}`}
              onClick={() => void openReaderFromTopbar()}
              aria-label={readerOpen ? '收起阅读器' : '展开阅读器'}
              aria-pressed={readerOpen}
              title={readerOpen ? '收起阅读器' : '展开阅读器'}
              disabled={mode === 'practice'}
            >
              <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
                <path d="M12 6c-1.8-1.6-4.2-2-7-2v14c2.8 0 5.2.4 7 2 1.8-1.6 4.2-2 7-2V4c-2.8 0-5.2.4-7 2z" />
                <path d="M12 6v14" />
              </svg>
            </button>
          </div>
          {mode === 'practice' && <PracticeBar />}
          <GraphCanvas />
        </>
      )}
      <SettingsPanel />
      <ReaderPanel />
      <Toasts />
    </div>
  )
}
```

- [ ] **Step 2: `vite.config.ts` 整体替换**

```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:8000',
      '/explain-api': {
        target: 'http://localhost:8100',
        rewrite: (p) => p.replace(/^\/explain-api/, '/api'),
      },
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: 'src/test/setup.ts',
    css: false
  }
})
```

- [ ] **Step 3: `RepoPage.tsx` 接入**

1. 删除 `import explainPlaceholder from '../assets/explain-placeholder.png'`，新增 `import ExplainPanel from '../explain/ExplainPanel'`；
2. `tab === 'explain'` 分支整体替换：

```tsx
              {tab === 'explain' && <ExplainPanel repo={detail} />}
```

- [ ] **Step 4: `RepoPage.css` 清理占位样式**

删除 `.explain-placeholder`、`.explain-img`、`.explain-title`、`.explain-sub` 四条规则（仅被占位块使用）。

- [ ] **Step 5: 更新 `RepoPage.test.tsx` 两个用例**

`切换到解读占位` 用例（原 45–50 行）整体替换为：

```tsx
  it('切换到解读面板，后端不可达时显示错误降级', async () => {
    vi.stubGlobal('fetch', vi.fn(() => Promise.reject(new TypeError('mock: offline'))))
    renderAt('/repo/1')
    await screen.findAllByText('README.md')
    await userEvent.click(screen.getByRole('tab', { name: '仓库解读' }))
    expect(await screen.findByText('解读服务暂不可用')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: '重试' })).toBeInTheDocument()
    expect(await screen.findByTestId('params')).toHaveTextContent('tab=explain')
    vi.unstubAllGlobals()
  })
```

`?tab=explain 深链直达解读占位` 用例（原 139–142 行）整体替换为：

```tsx
  it('?tab=explain 深链直达解读面板', async () => {
    vi.stubGlobal('fetch', vi.fn(() => Promise.reject(new TypeError('mock: offline'))))
    renderAt('/repo/1?tab=explain')
    expect(await screen.findByText('解读服务暂不可用')).toBeInTheDocument()
    vi.unstubAllGlobals()
  })
```

- [ ] **Step 6: 运行页面测试**

Run(workdir: frontend): `npx vitest run src/pages/RepoPage.test.tsx`
Expected: 全部 PASS（其余用例不受影响）

- [ ] **Step 7: Commit**

```bash
git add frontend/src/explain/ExplainPanel.tsx frontend/src/pages/RepoPage.tsx frontend/src/pages/RepoPage.css frontend/src/pages/RepoPage.test.tsx frontend/vite.config.ts
git commit -m "feat(frontend): 仓库解读 tab 接入 ExplainPanel（自动建图/骨架/错误降级）+ /explain-api 代理"
```

---

### Task 8: 全量校验

**Files:** 无新增（验证任务）

- [ ] **Step 1: 前端全量**

Run(workdir: frontend): `npm run typecheck` → Expected: 0 error
Run(workdir: frontend): `npm run test` → Expected: 全部 PASS

- [ ] **Step 2: 后端全量**

Run(workdir: backend): `python -m pytest` → Expected: 全部 PASS

- [ ] **Step 3: 手动 mock 冒烟（LLM_MOCK=true）**

1. 起 `backend`（`$env:LLM_MOCK='true'; python -m uvicorn app.main:app --port 8100`）与 `frontend`（`npm run dev`）；
2. 打开 `/repo/1?tab=explain`：骨架屏 → 画布出现中心节点"octocat/… 仓库的解读"（mock 语境为 demo 仓库）与首层子节点；
3. 依次验收：点击节点展开（受设置数约束）→ 点边看双向气泡 → 双击节点阅读器出精读并可追问 → 设置面板改"单次展开数/最高层数"即时生效 → 练习模式正文抹去、按路径重现、可退出；
4. 节点为暖色梯度、画布白底、连线米灰可辨；无灌木/输入框/morph 残留；
5. 停 `backend` 刷新页面：错误降级卡出现且"重试"可用（重启后端后重试成功出图）。

- [ ] **Step 4: 真实模式抽查（可选，配 LLM_API_KEY 与 GITHUB_TOKEN 后）**

`LLM_MOCK=false` 起后端，用任一公开仓库（如 `fastapi/full-stack-fastapi-template`）走一遍 Task 8 Step 3 的 1–4，确认子节点贴合仓库实情（提及实际模块/技术栈）。

- [ ] **Step 5: 收尾提交（若校验中产生修正）**

```bash
git add <仅本次修正的文件>
git commit -m "fix(frontend): 仓库解读迁移校验修正"
```
