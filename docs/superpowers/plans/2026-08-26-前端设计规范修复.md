# 前端设计规范修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 Web Interface Guidelines 审查结论，修复觅码前端 10 类问题：弹层可达性、悬停重叠、顶栏/搜索合规、URL 状态、错误恢复、标题层级与跳转链接、仓库页深链与交互回滚、个人页编辑、分页/树/分类栏细节、全局润色。

**Architecture:** 全部改动限于 `frontend/src`，React 组件 + 少量 CSS + 既存测试体系（vitest + testing-library + jsdom，`npx vitest run`）。URL 状态用 `useSearchParams`（与 CategoryBar/ProfilePage 既有模式一致）；错误恢复用 try/catch/finally + 请求令牌（与 RepoPage 既有 `fileReqRef` 模式一致）。数据仍走 mockClient。

**Tech Stack:** React 18、react-router-dom 6、zustand、vite 5、vitest 2、@testing-library/react 16。

## Global Constraints

- 界面文案一律简体中文；错误文案给下一步行动（"请重试"），不裸抛 `e.message`
- 颜色/圆角/阴影/字号只走 `styles/theme.css` 语义 token，组件禁止新增裸色值
- 动效遵守规范 §4.5：只动 `transform`/`opacity`，过渡列属性；`prefers-reduced-motion` 已全局降级，不得绕过
- 型名/命名沿用 BEM 既有类名；新测试并入同名 `.test.tsx`，沿用 `renderAt`/fixture 模式
- 每任务独立可测可提交；提交粒度：单任务一 commit，前缀 `fix(frontend):`
- 动手前先提交当前未提交基线快照（`wip(frontend): 基线快照`），避免与终审打磨改动混在一起

---

### Task 1: LoginModal 键盘可达性（Esc / 关闭按钮 / 焦点管理 / 滚动锁）

**Files:**
- Modify: `frontend/src/components/LoginModal.tsx`
- Modify: `frontend/src/components/LoginModal.css`
- Modify: `frontend/src/components/LoginModal.test.tsx`

**Interfaces:**
- Consumes: `useAuthStore.login`、`Button`（不变）
- Produces: `LoginModal({ open: boolean, onClose: () => void })` 新增行为：打开时焦点移入弹层首按钮、Esc 关闭、Tab 圈定、body 滚动锁、关闭时归还焦点

- [ ] **Step 1: 写失败测试**（追加到 `LoginModal.test.tsx`）

```tsx
it('打开时焦点移入弹层', () => {
  render(<LoginModal open onClose={() => {}} />)
  expect(screen.getByRole('button', { name: '用 GitHub 登录' })).toHaveFocus()
})
it('Esc 关闭弹层', async () => {
  const onClose = vi.fn()
  render(<LoginModal open onClose={onClose} />)
  await userEvent.keyboard('{Escape}')
  expect(onClose).toHaveBeenCalled()
})
it('有关闭按钮并响应点击', async () => {
  const onClose = vi.fn()
  render(<LoginModal open onClose={onClose} />)
  await userEvent.click(screen.getByRole('button', { name: '关闭登录弹层' }))
  expect(onClose).toHaveBeenCalled()
})
it('打开时锁定滚动，关闭后恢复', () => {
  const { unmount } = render(<LoginModal open onClose={() => {}} />)
  expect(document.body.style.overflow).toBe('hidden')
  unmount()
  expect(document.body.style.overflow).toBe('')
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/LoginModal.test.tsx`
Expected: FAIL（无关闭按钮、无 Esc 监听，焦点不在弹层内，body 无锁定）

- [ ] **Step 3: 实现**（重写 `LoginModal.tsx` 与补 CSS）

```tsx
// src/components/LoginModal.tsx —— 规范 §8.3 + 可达性：焦点圈定/Esc/关闭按钮/滚动锁
import { useEffect, useRef } from 'react'
import { useAuthStore } from '../store/authStore'
import Button from './Button'
import loginLogo from '../assets/login-logo.png'
import './LoginModal.css'

interface Props {
  open: boolean
  onClose: () => void
}

export default function LoginModal({ open, onClose }: Props) {
  const login = useAuthStore((s) => s.login)
  const modalRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (!open) return
    const prev = document.activeElement as HTMLElement | null
    const first = modalRef.current?.querySelector<HTMLElement>('button')
    first?.focus()
    const prevOverflow = document.body.style.overflow
    document.body.style.overflow = 'hidden'
    function onKey(e: KeyboardEvent) {
      if (e.key === 'Escape') { onClose(); return }
      if (e.key === 'Tab' && modalRef.current) {
        const focusables = modalRef.current.querySelectorAll<HTMLElement>('button')
        if (focusables.length === 0) return
        const firstF = focusables[0]
        const lastF = focusables[focusables.length - 1]
        if (e.shiftKey && document.activeElement === firstF) { e.preventDefault(); lastF.focus() }
        else if (!e.shiftKey && document.activeElement === lastF) { e.preventDefault(); firstF.focus() }
      }
    }
    document.addEventListener('keydown', onKey)
    return () => {
      document.removeEventListener('keydown', onKey)
      document.body.style.overflow = prevOverflow
      prev?.focus()
    }
  }, [open, onClose])

  if (!open) return null
  return (
    <div className="login-mask" onClick={onClose}>
      <div
        className="login-modal"
        role="dialog"
        aria-modal="true"
        aria-label="登录"
        aria-describedby="login-tip"
        ref={modalRef}
        onClick={(e) => e.stopPropagation()}
      >
        <img className="login-logo-img" src={loginLogo} alt="" aria-hidden="true" />
        <p className="login-tip" id="login-tip">登录后即可收藏、点赞与推广仓库</p>
        <Button onClick={() => { login(); onClose() }}>用 GitHub 登录</Button>
        <button className="login-close" aria-label="关闭登录弹层" onClick={onClose}>✕</button>
      </div>
    </div>
  )
}
```

`LoginModal.css` 追加：

```css
.login-modal { position: relative; overscroll-behavior: contain; }
.login-close {
  position: absolute; top: 12px; right: 12px;
  width: 28px; height: 28px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 50%;
  color: var(--text3);
  font-size: 14px;
  transition: background-color var(--time-fast), color var(--time-fast);
}
.login-close:hover { background: var(--brand_blue_thin); color: var(--brand_blue); }
```

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/components/LoginModal.test.tsx`
Expected: PASS（4 个新测试 + 原 3 个）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/LoginModal.tsx frontend/src/components/LoginModal.css frontend/src/components/LoginModal.test.tsx
git commit -m "fix(frontend): 登录弹层键盘可达性（Esc/关闭按钮/焦点圈定/滚动锁）"
```

---

### Task 2: RepoCard 悬停遮罩——统计与常驻 star 胶囊重叠 + 键盘/触屏可见

**Files:**
- Modify: `frontend/src/components/RepoCard.css`
- Modify: `frontend/src/components/RepoCard.test.tsx`（补结构断言）

**Interfaces:**
- Consumes: —（纯样式）
- Produces: 悬停或键盘聚焦封面时：`star-pill` 淡出、`hover-mask` 淡入（同一位置不重叠）；`hover-mask` 以 `:focus-within` 变体覆盖键盘与触屏

- [ ] **Step 1: 写（结构）测试**（追加 `RepoCard.test.tsx`）

```tsx
it('常驻统计与悬停遮罩位于同一封面内（互斥显示由 CSS 控制）', () => {
  renderCard(card)
  const cover = screen.getByRole('link', { name: 'mini-agent' })
  const pill = cover.querySelector('.star-pill')
  const mask = cover.querySelector('.hover-mask')
  expect(pill).toBeTruthy()
  expect(mask).toBeTruthy()
  expect(mask).toHaveTextContent('3100')
  expect(mask).toHaveTextContent('45')
})
```

- [ ] **Step 2: 运行确认**

Run: `npx vitest run src/components/RepoCard.test.tsx`
Expected: 新测试通过（结构已存在）。CSS 重叠修复无法在 jsdom 验证，人工确认样式表输出。

- [ ] **Step 3: 实现**（修改 `RepoCard.css`）

```css
.star-pill {
  position: absolute;
  left: 8px; bottom: 8px;
  display: inline-flex;
  align-items: center;
  gap: 5px;
  padding: 3px 10px;
  border-radius: 999px;
  background: var(--mask_cover);
  color: var(--bg1);
  font-family: var(--font-mono);
  font-size: 12px;
  transition: opacity var(--time-fast) var(--ease);   /* 新增：悬停时淡出 */
}
/* ...原有 .star-icon/.lang-dot 不变... */
.hover-mask {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: flex-end;
  gap: 16px;
  padding: 10px 12px;
  background: linear-gradient(transparent, var(--mask_cover));
  color: var(--bg1);
  font-size: 13px;
  opacity: 0;
  transition: opacity var(--time-slow) var(--ease);
}
/* 悬停或键盘聚焦（触屏 tap 聚焦）时：常驻胶囊淡出、统计遮罩淡入，互斥避免重叠 */
.repo-card-cover:hover .star-pill,
.repo-card-cover:focus-within .star-pill { opacity: 0; }
.repo-card-cover:hover .hover-mask,
.repo-card-cover:focus-within .hover-mask { opacity: 1; }
```

（删除原 `.repo-card-cover:hover .hover-mask { opacity: 1; }`，合入上面规则）

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/RepoCard.css frontend/src/components/RepoCard.test.tsx
git commit -m "fix(frontend): 卡片悬停统计与常驻胶囊互斥显示，补键盘/触屏可见与结构断言"
```

---

### Task 3: TopBar——导航高亮派生、搜索框合规（name/autocomplete/…）、保留查询词

**Files:**
- Modify: `frontend/src/components/TopBar.tsx`
- Modify: `frontend/src/components/TopBar.test.tsx`（placeholder 更新 + 2 个新测试）

**Interfaces:**
- Consumes: `useSearchParams`、`useLocation`、`NavLink`（react-router-dom）
- Produces: TopBar 在 `/search` 下以 URL `q` 初始化/同步输入框；导航项按当前路由高亮

- [ ] **Step 1: 写失败测试**（追加 `TopBar.test.tsx`）

```tsx
it('搜索页顶栏保留查询词（规范 §7.2）', () => {
  render(
    <MemoryRouter initialEntries={['/search?q=agent']}>
      <Routes><Route path="*" element={<TopBar />} /></Routes>
    </MemoryRouter>,
  )
  expect(screen.getByRole('textbox', { name: '搜索' })).toHaveValue('agent')
})

it('导航高亮仅首页', () => {
  render(
    <MemoryRouter initialEntries={['/repo/1']}>
      <Routes><Route path="*" element={<TopBar />} /></Routes>
    </MemoryRouter>,
  )
  expect(screen.getByRole('link', { name: '首页' })).not.toHaveClass('is-current')
})
```

并修改既有占位符断言：`getByPlaceholderText('搜索仓库、标签、一句话卖点')` → `getByPlaceholderText('搜索仓库、标签、一句话卖点…')`

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/TopBar.test.tsx`
Expected: FAIL（占位符无省略号、搜索框无初始值、首页恒 is-current）

- [ ] **Step 3: 实现**（修改 `TopBar.tsx`）

```tsx
import { useEffect, useState } from 'react'
import { Link, NavLink, useLocation, useNavigate, useSearchParams } from 'react-router-dom'
// ...其余 import 不变
export default function TopBar() {
  const [q, setQ] = useState('')
  const [loginOpen, setLoginOpen] = useState(false)
  const navigate = useNavigate()
  const [params] = useSearchParams()
  const location = useLocation()
  const user = useAuthStore((s) => s.user)

  // 搜索结果页顶栏保留查询词（规范 §7.2）；其余页不随 URL 回写
  useEffect(() => {
    if (location.pathname === '/search') setQ(params.get('q') ?? '')
  }, [location.pathname, params])
```

导航与输入框替换：

```tsx
<nav className="topbar-nav">
  <NavLink
    className={({ isActive }) => `nav-item${isActive ? ' is-current' : ''}`}
    to="/"
    end
  >首页</NavLink>
</nav>
```

```tsx
<input
  className="search-input"
  name="q"
  autoComplete="off"
  spellCheck={false}
  value={q}
  onChange={(e) => setQ(e.target.value)}
  onKeyDown={(e) => e.key === 'Enter' && submitSearch()}
  placeholder="搜索仓库、标签、一句话卖点…"
  aria-label="搜索"
/>
```

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/components/TopBar.test.tsx`
Expected: PASS（8 项）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/TopBar.tsx frontend/src/components/TopBar.test.tsx
git commit -m "fix(frontend): 顶栏导航高亮派生、搜索框合规、搜索页保留查询词"
```

---

### Task 4: SearchPage——排序/页码入 URL、错误恢复、空关键词保护、竞态防护

**Files:**
- Modify: `frontend/src/pages/SearchPage.tsx`
- Modify: `frontend/src/pages/SearchPage.test.tsx`

**Interfaces:**
- Consumes: `api.search(q, sort, page)`、`ErrorBanner`、`EmptyState`
- Produces: `?sort=`、`?page=` 参数；`sort`/`page` 各由 URL 驱动（q 变化重置 page）；失败时 `ErrorBanner` + 重试；空 q 显示引导空态且不卡加载

- [ ] **Step 1: 写失败测试**（追加 `SearchPage.test.tsx`；`renderAt` 内建参数探针）

```tsx
function ParamProbe() {
  const [p] = useSearchParams()
  return <output data-testid="params">{p.toString()}</output>
}
```
（`renderAt` 修改：`<Route path="/search" element={<><SearchPage /><ParamProbe /></>} />`；`renderAtWithNav` 同样内嵌探针）

```tsx
it('排序与页码写入 URL', async () => {
  renderAt('/search?q=a')
  await screen.findByTestId('search-card-1')
  await userEvent.click(screen.getByRole('tab', { name: '最新' }))
  expect(await screen.findByTestId('params')).toHaveTextContent('sort=newest')
})

it('搜索失败显示错误条并可重试', async () => {
  const { api } = await import('../api/client')
  const spy = vi.spyOn(api, 'search').mockRejectedValueOnce(new Error('boom'))
  renderAt('/search?q=agent')
  expect(await screen.findByText('搜索失败，请重试')).toBeInTheDocument()
  expect(screen.getByRole('button', { name: '重试' })).toBeInTheDocument()
  spy.mockRestore()
})

it('空关键词显示引导且不卡加载', async () => {
  renderAt('/search?q=')
  expect(await screen.findByText('在顶栏输入关键词开始搜索')).toBeInTheDocument()
  expect(screen.queryByText('搜索中…')).not.toBeInTheDocument()
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/SearchPage.test.tsx`
Expected: FAIL（当前 sort/page 未写 URL；失败时卡"搜索中…"；空 q 卡加载）

- [ ] **Step 3: 实现**（重写关键状态与 run，`SearchPage.tsx`）

```tsx
import { useCallback, useEffect, useRef, useState } from 'react'
// ...新增 import：ErrorBanner
const [params, setParams] = useSearchParams()
const q = params.get('q') ?? ''
const sort = (params.get('sort') as SortKey) || 'default'
const page = Math.max(1, Number(params.get('page')) || 1)
const [cards, setCards] = useState<RepoCardData[]>([])
const [total, setTotal] = useState(0)
const [loading, setLoading] = useState(true)
const [error, setError] = useState<string | null>(null)
const reqRef = useRef(0)

const run = useCallback(async () => {
  if (!q) { setCards([]); setTotal(0); setLoading(false); return }
  const req = ++reqRef.current
  setLoading(true)
  setError(null)
  try {
    const res = await api.search(q, sort, page)
    if (req !== reqRef.current) return
    setCards(res.cards)
    setTotal(res.total)
  } catch {
    if (req !== reqRef.current) return
    setCards([])
    setTotal(0)
    setError('搜索失败，请重试')
  } finally {
    if (req === reqRef.current) setLoading(false)
  }
}, [q, sort, page])

useEffect(() => { void run() }, [run])

// q 变化重置页码（沿既有"换词回第 1 页"行为）
useEffect(() => {
  if (q && page !== 1) {
    const p = new URLSearchParams(params)
    p.delete('page')
    setParams(p, { replace: true })
  }
}, [q, page, params, setParams])

function changeSort(k: string) {
  const p = new URLSearchParams(params)
  p.set('sort', k)
  p.delete('page')
  setParams(p)
}
function changePage(n: number) {
  const p = new URLSearchParams(params)
  p.set('page', String(n))
  setParams(p)
}
```

渲染部分替换：

```tsx
{!q && <EmptyState title="在顶栏输入关键词开始搜索" />}
{q && (
  <p className="search-summary" aria-live="polite">搜索「{q}」· 共 {total} 个结果</p>
)}
{q && (
  <Tabs items={...} active={sort} onChange={changeSort} />
)}
{error && <ErrorBanner message={error} onRetry={() => void run()} />}
{loading && !error && <p className="search-loading">搜索中…</p>}
{!loading && !error && q && total === 0 && ( ...空态... )}
{!loading && !error && cards.length > 0 && ( ...列表... )}
{!error && <Pagination page={page} total={total} pageSize={PAGE_SIZE} onChange={changePage} />}
```

（Tabs 的 items 与 SORTS 常量不变；`useState<SortKey>`/`useState(1)` 删除）

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/pages/SearchPage.test.tsx`
Expected: PASS（原 5 项 + 新 3 项；"换关键词重置页码"行为保持）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages/SearchPage.tsx frontend/src/pages/SearchPage.test.tsx
git commit -m "fix(frontend): 搜索页排序/页码 URL 化，错误恢复与空关键词保护"
```

---

### Task 5: SubmitPage——AI/发布错误恢复、字数计数、单选语义与未保存提醒

**Files:**
- Modify: `frontend/src/pages/SubmitPage.tsx`
- Modify: `frontend/src/pages/SubmitPage.css`
- Modify: `frontend/src/pages/SubmitPage.test.tsx`

**Interfaces:**
- Consumes: `api.aiDraft(repoId)`、`api.submitRepo(payload)`、`Button`（loading）
- Produces: `actionError` 文案（role="alert"）；AI 失败按钮恢复；publish 失败按钮恢复；计数 `{length}/{TAGLINE_MAX}`；`aria-pressed` 分类；`beforeunload` 守卫

- [ ] **Step 1: 写失败测试**（追加 `SubmitPage.test.tsx`）

```tsx
it('AI 生成失败提示且不阻塞手写', async () => {
  useAuthStore.setState({ user: FIXTURE_USER })
  const { api } = await import('../api/client')
  const spy = vi.spyOn(api, 'aiDraft').mockRejectedValueOnce(new Error('boom'))
  renderPage()
  await userEvent.click(await screen.findByLabelText(/mini-agent/))
  await userEvent.click(screen.getByRole('button', { name: '下一步' }))
  await userEvent.click(screen.getByRole('button', { name: 'AI 帮我写' }))
  expect(await screen.findByRole('alert')).toHaveTextContent('AI 生成失败')
  expect(screen.getByRole('button', { name: 'AI 帮我写' })).toBeEnabled()
  spy.mockRestore()
})

it('发布失败显示提示且按钮恢复', async () => {
  useAuthStore.setState({ user: FIXTURE_USER })
  const { api } = await import('../api/client')
  const spy = vi.spyOn(api, 'submitRepo').mockRejectedValueOnce(new Error('boom'))
  renderPage()
  await userEvent.click(await screen.findByLabelText(/mini-agent/))
  await userEvent.click(screen.getByRole('button', { name: '下一步' }))
  await userEvent.type(screen.getByLabelText('一句话卖点'), '手写卖点')
  await userEvent.click(screen.getByRole('button', { name: '发布' }))
  expect(await screen.findByRole('alert')).toHaveTextContent('发布失败')
  expect(screen.getByRole('button', { name: '发布' })).toBeEnabled()
  spy.mockRestore()
})

it('卖点字数计数实时更新', async () => {
  useAuthStore.setState({ user: FIXTURE_USER })
  renderPage()
  await userEvent.click(await screen.findByLabelText(/mini-agent/))
  await userEvent.click(screen.getByRole('button', { name: '下一步' }))
  const input = screen.getByLabelText('一句话卖点')
  await userEvent.type(input, '5 个字')
  expect(screen.getByText('5/40')).toBeInTheDocument()
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/SubmitPage.test.tsx`
Expected: FAIL（无 alert；失败后按钮卡死；无计数）

- [ ] **Step 3: 实现**（`SubmitPage.tsx`）

```tsx
const [actionError, setActionError] = useState<string | null>(null)

async function aiDraft() {
  if (!picked) return
  setDrafting(true)
  setActionError(null)
  try {
    const draft = await api.aiDraft(picked.id)
    setTagline(draft.tagline_zh.slice(0, TAGLINE_MAX))
    setIntro(draft.intro_zh)
  } catch {
    setActionError('AI 生成失败，可先手写，稍后再试')
  } finally {
    setDrafting(false)
  }
}

async function publish() {
  if (!picked || !tagline.trim()) return
  setPublishing(true)
  setActionError(null)
  try {
    await api.submitRepo({ ... })
    setStep(2)
  } catch {
    setActionError('发布失败，请重试')
  } finally {
    setPublishing(false)
  }
}

// 未保存变更离开提醒（步 2 有编辑内容时）
useEffect(() => {
  if (step !== 1) return
  const dirty = tagline.trim().length > 0 || intro.trim().length > 0
  if (!dirty) return
  const onBeforeUnload = (e: BeforeUnloadEvent) => { e.preventDefault(); e.returnValue = '' }
  window.addEventListener('beforeunload', onBeforeUnload)
  return () => window.removeEventListener('beforeunload', onBeforeUnload)
}, [step, tagline, intro])
```

表单替换要点：

```tsx
<label className="field">
  <span className="field-label">
    一句话卖点（必填）
    <span className="tagline-count" aria-live="polite">（{tagline.length}/{TAGLINE_MAX}）</span>
  </span>
  <input name="tagline" autoComplete="off" value={tagline} maxLength={TAGLINE_MAX}
    onChange={(e) => setTagline(e.target.value)} aria-label="一句话卖点" />
</label>
<label className="field">
  <span className="field-label">详细介绍</span>
  <textarea name="intro" autoComplete="off" rows={6} value={intro} onChange={(e) => setIntro(e.target.value)} aria-label="详细介绍" />
</label>
<div className="field" role="group" aria-label="分类">
  <span className="field-label">分类</span>
  <div className="cat-pills">
    {cats.map((c) => (
      <button key={c} className={`cat-pill ${category === c ? 'is-active' : ''}`}
        aria-pressed={category === c} onClick={() => setCategory(c)}>
        {c}
      </button>
    ))}
  </div>
</div>
{actionError && <p className="action-error" role="alert">{actionError}</p>}
```

`SubmitPage.css` 追加：

```css
.tagline-count { margin-left: 6px; color: var(--text3); font-family: var(--font-mono); font-size: 12px; }
.action-error { color: var(--stress_red); font-size: 13px; }
```

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/pages/SubmitPage.test.tsx`
Expected: PASS（原 4 项 + 新 3 项）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages/SubmitPage.tsx frontend/src/pages/SubmitPage.css frontend/src/pages/SubmitPage.test.tsx
git commit -m "fix(frontend): 投稿向导错误恢复、卖点计数与语义补全"
```

---

### Task 6: 全站标题层级/跳转链接/错误可见性（App + Home + 五页 main id）

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/styles/base.css`
- Modify: `frontend/src/pages/HomePage.tsx`、`HomePage.css`（如需）
- Modify: `frontend/src/pages/SearchPage.tsx`、`RepoPage.tsx`、`SubmitPage.tsx`、`ProfilePage.tsx`（各 `main` 加 `id="main"`）
- Modify: `frontend/src/pages/HomePage.test.tsx`、`frontend/src/App.test.tsx`

**Interfaces:**
- Consumes: ——
- Produces: 全局 skip link（`#main`）；各页 `<main id="main">`；HomePage 视觉隐藏 h1；翻页失败错误条在已有卡片时仍可见

- [ ] **Step 1: 写失败测试**

`App.test.tsx` 追加：

```tsx
it('提供跳转主内容链接', () => {
  renderAt('/')
  expect(screen.getByRole('link', { name: '跳转到主要内容' })).toHaveAttribute('href', '#main')
})
```

`HomePage.test.tsx` 追加 + 修改：

```tsx
it('页面提供 h1 标题', async () => {
  render(<HomePage />, { wrapper: MemoryRouter })
  await screen.findAllByText('mini-agent')
  expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('觅码')
})

it('翻页失败时错误条仍可见（已有卡片）', async () => {
  useFeedStore.setState({
    cards: [FIXTURE_REPOS[0]], loading: false, error: '某错误',
    hasMore: false, page: 1, category: null,
  })
  render(<HomePage />, { wrapper: MemoryRouter })
  expect(await screen.findByText('加载失败，请重试')).toBeInTheDocument()
})
```

（修改既有错误态测试期望文案：`'网络开小差了'` → `'加载失败，请重试'`；import `FIXTURE_REPOS`）

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/App.test.tsx src/pages/HomePage.test.tsx`
Expected: FAIL（无 skip link、无 h1、错误文案不符合、有卡片时无错误条）

- [ ] **Step 3: 实现**

`App.tsx`：

```tsx
<>
  <a className="skip-link" href="#main">跳转到主要内容</a>
  <Routes>...</Routes>
  <SiteFooter />
</>
```

`base.css` 追加：

```css
/* 跳转主内容链接：仅聚焦时可见（可达性） */
.skip-link {
  position: absolute;
  left: 16px; top: -48px;
  z-index: 200;
  padding: 10px 16px;
  background: var(--brand_blue);
  color: var(--bg1);
  border-radius: var(--radius-md);
  transition: top var(--time-fast);
}
.skip-link:focus-visible { top: 12px; }
/* 仅供屏幕阅读器的文本（页级 h1 等） */
.visually-hidden {
  position: absolute;
  width: 1px; height: 1px;
  padding: 0; margin: -1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
  white-space: nowrap;
  border: 0;
}
```

`HomePage.tsx`：

```tsx
<>
  <TopBar />
  <CategoryBar />
  <h1 className="visually-hidden">觅码 · 发现潜力开源仓库</h1>
  <main id="main" className="page-shell">
    {error && (
      <ErrorBanner message="加载失败，请重试"
        onRetry={() => void load(useFeedStore.getState().category)} />
    )}
    ...
```

（其余页 `<main className="page-shell">` → `<main id="main" className="page-shell">`；RepoPage/SubmitPage/ProfilePage 的每个渲染分支同样处理；SearchPage 在 `<main>` 前加 `<h1 className="visually-hidden">搜索「{q}」</h1>`）

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/App.test.tsx src/pages/HomePage.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add -A frontend/src/App.tsx frontend/src/styles/base.css frontend/src/pages/HomePage.tsx frontend/src/pages/SearchPage.tsx frontend/src/pages/RepoPage.tsx frontend/src/pages/SubmitPage.tsx frontend/src/pages/ProfilePage.tsx frontend/src/pages/HomePage.test.tsx frontend/src/App.test.tsx
git commit -m "fix(frontend): 全站跳转链接与标题层级，翻页错误可见"
```

---

### Task 7: RepoPage——tab/文件深链、交互失败回滚与加载态、树加载降级、CodeView 上限与分支链接

**Files:**
- Modify: `frontend/src/api/types.ts`（`RepoDetail.default_branch: string`）
- Modify: `frontend/src/api/mockClient.ts`（`toDetail` 补 `default_branch: 'main'`）
- Modify: `frontend/src/pages/RepoPage.tsx`
- Modify: `frontend/src/components/CodeView.tsx`、`CodeView.css`
- Modify: `frontend/src/pages/RepoPage.test.tsx`

**Interfaces:**
- Consumes: `api.repo/repoTree/repoFile/interact`
- Produces: `?tab=files|explain`、`?file=<path>`；`RepoDetail.default_branch`；`CodeView` 前 2000 行上限与 `blob/<branch>` 链接

- [ ] **Step 1: 写失败测试**（追加 `RepoPage.test.tsx`；renderAt 内嵌 ParamProbe）

```tsx
it('tab 切换写入 URL', async () => {
  renderAt('/repo/1')
  await screen.findAllByText('README.md')
  await userEvent.click(screen.getByRole('tab', { name: '仓库解读' }))
  expect(await screen.findByTestId('params')).toHaveTextContent('tab=explain')
})

it('?file= 深链直接加载指定文件', async () => {
  renderAt('/repo/1?file=src/main.py')
  expect(await screen.findByTestId('code-content')).toHaveTextContent('from loop import run')
})

it('点赞失败回滚激活态并提示', async () => {
  useAuthStore.setState({ user: FIXTURE_USER })
  const { api } = await import('../api/client')
  const spy = vi.spyOn(api, 'interact').mockRejectedValueOnce(new Error('boom'))
  renderAt('/repo/2')
  await screen.findByText('tinyfetch')
  const btn = screen.getByRole('button', { name: '点赞' })
  await userEvent.click(btn)
  await waitFor(() => expect(btn).not.toHaveClass('is-on'))
  expect(screen.getByRole('alert')).toHaveTextContent('操作失败')
  spy.mockRestore()
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/RepoPage.test.tsx`
Expected: FAIL（tab 不写 URL；文件选择不写 URL 但深链本就该工作——实现后行为成立；interact 失败无回滚）

- [ ] **Step 3: 实现**

`types.ts`：`RepoDetail` 增 `default_branch: string`。
`mockClient.ts` `toDetail`：`default_branch: 'main'`。

`RepoPage.tsx` 要点：

```tsx
const [params, setParams] = useSearchParams()
const tab = params.get('tab') === 'explain' ? 'explain' : 'files'
const fileParam = params.get('file')
const [actionError, setActionError] = useState<string | null>(null)
const [busy, setBusy] = useState<'like' | 'fav' | null>(null)
const [treeError, setTreeError] = useState(false)

// 树加载：URL 指定文件交给 param effect；否则默认第一个文件
api.repoTree(repoId).then((t) => {
  if (!alive) return
  setTree(t)
  if (fileParam) return
  const first = t.find((n) => n.type === 'file')
  if (first) void selectFile(first.path)
}).catch(() => { if (alive) setTreeError(true) })

// URL 文件深链（切换文件由 onSelectFile 写回）
useEffect(() => {
  if (tab === 'files' && fileParam) void selectFile(fileParam)
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [fileParam, tab, repoId])

function switchTab(k: string) {
  const p = new URLSearchParams(params)
  p.set('tab', k)
  setParams(p)
}
function onSelectFile(p: string) {
  const n = new URLSearchParams(params)
  n.set('file', p)
  setParams(n, { replace: true })
}

async function toggleLike() {
  if (busy) return
  const next = !liked
  setBusy('like'); setActionError(null)
  setLiked(next)
  try {
    await api.interact(repoId, 'like', next)
    setDetail((d) => d ? { ...d, likes: d.likes + (next ? 1 : -1) } : d)
  } catch {
    setLiked(!next)
    setActionError('操作失败，请重试')
  } finally { setBusy(null) }
}
// toggleFav 类同（回滚 setFaved(!next)）
```

渲染替换：`<Tabs items={TAB_ITEMS} active={tab} onChange={switchTab} />`；`<div className="repo-tab-body" key={tab} role="tabpanel" aria-label={tab === 'files' ? '文件预览' : '仓库解读'}>`；`FileTree onSelect={(p) => onSelectFile(p)}`；文件树降级 `{treeError && <p className="file-fallback">文件树加载失败，<a href={detail.github_url} target="_blank" rel="noreferrer">去 GitHub 查看 ↗</a></p>}`；按钮组加 `{actionError && <p className="action-error" role="alert">{actionError}</p>}`，`<Button ... loading={busy === 'like'}>` / `loading={busy === 'fav'}`；`CodeView` 传入 `defaultBranch={detail.default_branch}`。

`CodeView.tsx`：

```tsx
const MAX_LINES = 2000
export default function CodeView({ file, githubUrl, defaultBranch = 'main' }: { file: RepoFile; githubUrl: string; defaultBranch?: string }) {
  const lines = file.content.split('\n')
  const shown = lines.length > MAX_LINES ? lines.slice(0, MAX_LINES) : lines
  return (
    <div className="code-view">
      <div className="code-head">
        <span className="code-path">{file.path}</span>
        <a className="code-gh-link" href={`${githubUrl}/blob/${defaultBranch}/${file.path}`} target="_blank" rel="noreferrer">
          在 GitHub 查看 ↗
        </a>
      </div>
      <pre className="code-body" data-testid="code-content">
        {shown.map((line, i) => (
          <span key={i} className="code-line">
            <span className="code-ln">{i + 1}</span>
            <span className="code-text">{line || ' '}</span>
          </span>
        ))}
      </pre>
      {lines.length > MAX_LINES && (
        <p className="code-truncated">仅展示前 {MAX_LINES} 行，完整代码请在 GitHub 查看</p>
      )}
    </div>
  )
}
```

`CodeView.css`：

```css
.code-line {
  display: flex;
  padding: 0 16px;
  content-visibility: auto;
  contain-intrinsic-size: auto 22px;
}
.code-truncated {
  padding: 8px 16px;
  font-size: 12px;
  color: var(--text3);
  border-top: 1px solid var(--line_light);
}
```

（`pre` 内 `div` → `span`：`pre` 只允许 phrasing 内容，`display:flex` 对 span 同样生效）

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/pages/RepoPage.test.tsx`
Expected: PASS（原 7 项 + 新 3 项）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/api/types.ts frontend/src/api/mockClient.ts frontend/src/pages/RepoPage.tsx frontend/src/components/CodeView.tsx frontend/src/components/CodeView.css frontend/src/pages/RepoPage.test.tsx
git commit -m "fix(frontend): 仓库页深链与交互回滚，代码视图上限与分支感知"
```

---

### Task 8: ProfilePage——头像语义、签名编辑键盘与可见性

**Files:**
- Modify: `frontend/src/pages/ProfilePage.tsx`
- Modify: `frontend/src/pages/ProfilePage.css`
- Modify: `frontend/src/pages/ProfilePage.test.tsx`

**Interfaces:**
- Consumes: ——
- Produces: 头像 `role="img"` + 名字标签；签名输入 Esc 取消、仅指针设备 autoFocus；铅笔按钮 `:focus-within`/`:focus-visible` 可见

- [ ] **Step 1: 写失败测试**

修改既有断言 `screen.getByLabelText('头像')` → `screen.getByRole('img', { name: 'alice 的头像' })`；追加：

```tsx
it('Esc 取消编辑签名', async () => {
  useAuthStore.setState({ user: FIXTURE_USER })
  renderAt('/user/alice')
  await screen.findAllByText('alice')
  await userEvent.click(screen.getByLabelText('编辑签名'))
  const input = screen.getByDisplayValue('在写小而可读的系统软件。')
  await userEvent.clear(input)
  await userEvent.type(input, '不保存的签名')
  await userEvent.keyboard('{Escape}')
  expect(screen.getByText('在写小而可读的系统软件。')).toBeInTheDocument()
  expect(screen.queryByDisplayValue('不保存的签名')).not.toBeInTheDocument()
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/ProfilePage.test.tsx`
Expected: FAIL（label 断言失败；Esc 无效）

- [ ] **Step 3: 实现**

`ProfilePage.tsx`：

```tsx
<div className="profile-avatar" role="img" aria-label={`${profile.login} 的头像`}>
```

```tsx
const finePointer = typeof window.matchMedia === 'function'
  ? window.matchMedia('(pointer: fine)').matches
  : true
...
<input
  className="bio-input"
  defaultValue={profile.bio}
  autoFocus={finePointer}
  onBlur={(e) => void saveBio(e.target.value)}
  onKeyDown={(e) => {
    if (e.key === 'Enter') void saveBio((e.target as HTMLInputElement).value)
    if (e.key === 'Escape') setEditingBio(false)
  }}
  aria-label="签名"
/>
```

（`finePointer` 在组件体内计算一次即可）

`ProfilePage.css`：

```css
.profile-bio:hover .bio-edit,
.profile-bio:focus-within .bio-edit,
.bio-edit:focus-visible { opacity: 1; }
```

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/pages/ProfilePage.test.tsx`
Expected: PASS（原 7 项 + 新 1 项）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages/ProfilePage.tsx frontend/src/pages/ProfilePage.css frontend/src/pages/ProfilePage.test.tsx
git commit -m "fix(frontend): 个人页头像语义与签名编辑键盘可达性"
```

---

### Task 9: Pagination/FileTree/CategoryBar/Tabs——大页码窗口化与 ARIA 补全

**Files:**
- Modify: `frontend/src/components/Pagination.tsx`、`Pagination.css`
- Modify: `frontend/src/components/FileTree.tsx`
- Modify: `frontend/src/components/CategoryBar.tsx`
- Modify: `frontend/src/components/Tabs.tsx`
- Modify: `frontend/src/components/Pagination.test.tsx`、`CategoryBar.test.tsx`、`Tabs.test.tsx`

**Interfaces:**
- Consumes: ——
- Produces: `Pagination` 窗口化（≤7 全显，否则 1 … n-1 n n+1 … N）且当前页 `aria-current="page"`；`FileTree` 当前文件 `aria-current="true"`；`CategoryBar` categories 失败兜底、`aria-expanded`、Esc/点击外部关闭；`Tabs` ←/→ 切换与 `aria-controls`

- [ ] **Step 1: 写失败测试**

`Pagination.test.tsx` 追加：

```tsx
it('页数很多时窗口化并以省略号收拢', () => {
  render(<Pagination page={13} total={200} pageSize={8} onChange={() => {}} />)
  expect(screen.getByRole('button', { name: '13' })).toHaveAttribute('aria-current', 'page')
  expect(screen.getByRole('button', { name: '1' })).toBeInTheDocument()
  expect(screen.getByRole('button', { name: '25' })).toBeInTheDocument()
  expect(screen.getAllByText('…').length).toBeGreaterThanOrEqual(2)
})
```

（既有首条测试补 `aria-current` 断言：`expect(screen.getByRole('button', { name: '1' })).toHaveAttribute('aria-current', 'page')`）

`CategoryBar.test.tsx` 追加：

```tsx
it('更多菜单支持 Esc 与外部点击关闭', async () => {
  const { api } = await import('../api/client')
  const spy = vi.spyOn(api, 'categories').mockResolvedValue(['一','二','三','四','五','六','七','八','九'])
  renderBar()
  const moreBtn = await screen.findByText('更多 ▾')
  expect(moreBtn).toHaveAttribute('aria-expanded', 'false')
  await userEvent.click(moreBtn)
  expect(moreBtn).toHaveAttribute('aria-expanded', 'true')
  await userEvent.keyboard('{Escape}')
  expect(screen.queryByText('九')).not.toBeInTheDocument()
  spy.mockRestore()
})
```

`Tabs.test.tsx` 追加：

```tsx
it('方向键切换 tab', async () => {
  const onChange = vi.fn()
  render(<Tabs items={[{ key: 'a', label: 'A' }, { key: 'b', label: 'B' }]} active="a" onChange={onChange} />)
  await userEvent.keyboard('{ArrowRight}')
  expect(onChange).toHaveBeenCalledWith('b')
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/Pagination.test.tsx src/components/CategoryBar.test.tsx src/components/Tabs.test.tsx`
Expected: FAIL

- [ ] **Step 3: 实现**

`Pagination.tsx`：

```tsx
function pageItems(page: number, pages: number): (number | '…')[] {
  if (pages <= 7) return Array.from({ length: pages }, (_, i) => i + 1)
  const keep = [...new Set([1, page - 1, page, page + 1, pages])]
    .filter((n) => n >= 1 && n <= pages).sort((a, b) => a - b)
  const out: (number | '…')[] = []
  let prev = 0
  for (const n of keep) {
    if (prev && n - prev > 1) out.push('…')
    out.push(n)
    prev = n
  }
  return out
}

export default function Pagination({ page, total, pageSize, onChange }: Props) {
  const pages = Math.ceil(total / pageSize)
  if (pages <= 1) return null
  return (
    <nav className="pagination" aria-label="分页">
      {pageItems(page, pages).map((p, i) =>
        p === '…' ? (
          <span key={`e${i}`} className="page-ellipsis" aria-hidden="true">…</span>
        ) : (
          <button
            key={p}
            className={`page-item ${p === page ? 'is-active' : ''}`}
            aria-current={p === page ? 'page' : undefined}
            onClick={() => onChange(p)}
          >
            {p}
          </button>
        ),
      )}
    </nav>
  )
}
```

`Pagination.css`：`.page-ellipsis { align-self: center; color: var(--text3); padding: 0 2px; }`

`FileTree.tsx` 文件节点：`aria-current={current === item.path ? 'true' : undefined}`。

`CategoryBar.tsx`：

```tsx
useEffect(() => { api.categories().then(setCats).catch(() => setCats([])) }, [])

useEffect(() => {
  if (!moreOpen) return
  function onDoc(e: MouseEvent) {
    if (!(e.target as HTMLElement).closest('.cat-more-wrap')) setMoreOpen(false)
  }
  function onKey(e: KeyboardEvent) { if (e.key === 'Escape') setMoreOpen(false) }
  document.addEventListener('click', onDoc)
  document.addEventListener('keydown', onKey)
  return () => {
    document.removeEventListener('click', onDoc)
    document.removeEventListener('keydown', onKey)
  }
}, [moreOpen])
```

`<button className="cat-pill" aria-expanded={moreOpen} aria-haspopup="menu" onClick={() => setMoreOpen((v) => !v)}>更多 ▾</button>`（菜单容器 `role="menu"` + 子项 `role="menuitem"` 可选，保持按钮语义即可）。

`Tabs.tsx`：tablist 加 `onKeyDown`（←/→ 换项），tab 加 `aria-controls={panelId}`：

```tsx
interface Props { items: { key: string; label: string; count?: number }[]; active: string; onChange: (key: string) => void; panelId?: string }
...
<div className="tabs" role="tablist" onKeyDown={(e) => {
  const idx = items.findIndex((i) => i.key === active)
  if (e.key === 'ArrowRight') { e.preventDefault(); onChange((items[idx + 1] ?? items[0]).key) }
  if (e.key === 'ArrowLeft') { e.preventDefault(); onChange((items[idx - 1] ?? items[items.length - 1]).key) }
}}>
  {items.map((item) => (
    <button key={item.key} role="tab" aria-selected={item.key === active} aria-controls={panelId} ...>
```

（RepoPage/ProfilePage 传 `panelId`，其 `*_tab-body` 加 `role="tabpanel"` + 对应 id；搜索页 Tabs 不传 panelId）

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/components/Pagination.test.tsx src/components/CategoryBar.test.tsx src/components/Tabs.test.tsx src/pages/RepoPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SearchPage.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/Pagination.tsx frontend/src/components/Pagination.css frontend/src/components/FileTree.tsx frontend/src/components/CategoryBar.tsx frontend/src/components/Tabs.tsx frontend/src/components/Pagination.test.tsx frontend/src/components/CategoryBar.test.tsx frontend/src/components/Tabs.test.tsx
git commit -m "fix(frontend): 分页窗口化与树/分类栏/tab ARIA 补全"
```

---

### Task 10: 全局润色——封面图片规范、长内容截断、触摸与字号细节、窄屏布局

**Files:**
- Modify: `frontend/src/components/RepoCover.tsx`、`RepoCover.test.tsx`
- Modify: `frontend/src/components/FeatureBar.tsx`（eager 封面）
- Modify: `frontend/src/components/Capsule.css`
- Modify: `frontend/src/components/RepoCard.css`、`SearchPage.css`、`RepoPage.css`、`TopBar.css`、`SubmitPage.css`、`ProfilePage.css`、`FeatureBar.css`
- Modify: `frontend/src/styles/base.css`
- Modify: `src/components/RepoCover.css`（如需）

**Interfaces:**
- Consumes: ——
- Produces: `RepoCover` 新增 `loading?: 'lazy' | 'eager'` prop 与 `width/height` 属性；长 topic 封面字号降档；全站 `touch-action`/tap-highlight；数值行 tabular-nums；标题 text-wrap: balance；窄屏（顶栏/搜索卡/仓库文件区/投稿编辑）布局修补

- [ ] **Step 1: 写失败测试**（追加 `RepoCover.test.tsx`）

```tsx
it('封面 img 带尺寸与懒加载', () => {
  const { container } = render(<RepoCover name="x" language={null} topics={[]} coverUrl="https://example.com/c.png" />)
  const img = container.querySelector('img') as HTMLImageElement
  expect(img).toHaveAttribute('width', '672')
  expect(img).toHaveAttribute('height', '378')
  expect(img).toHaveAttribute('loading', 'lazy')
})

it('长 topic 封面胶囊字号降档防溢出', () => {
  const { container } = render(<RepoCover name="x" language={null} topics={['machine-learning']} />)
  expect(container.querySelector('text[font-size="17"]')).toBeTruthy()
})
```

（`RepoCover` 用到 `Router` 吗？不——纯 svg/img，Render 无需 wrapper；若已用 MemoryRouter 则沿用）

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/RepoCover.test.tsx`
Expected: FAIL（无 width/height/loading 属性；字号恒 22）

- [ ] **Step 3: 实现**

`RepoCover.tsx`：

```tsx
interface Props {
  name: string
  language: string | null
  topics: string[]
  coverUrl?: string | null
  className?: string
  loading?: 'lazy' | 'eager'
}

// 封面内 topic 胶囊字号：按估算宽度降档（胶囊定宽 138，防文字溢出画布）
function capsuleFontSize(t: string): number {
  const est = [...t].reduce((acc, ch) => acc + (ch.charCodeAt(0) > 255 ? 22 : 12), 0)
  return est > 132 ? 17 : 22
}
```

`if (coverUrl)` 分支：

```tsx
return <img className={`repo-cover repo-cover-img ${className ?? ''}`} src={coverUrl} alt={name}
  width={672} height={378} loading={loading ?? 'lazy'} />
```

topics 渲染：

```tsx
{shown.map((t, i) => (
  <g key={t}>
    <rect x={48 + i * 150} y="312" width="138" height="40" rx="12" fill={capsuleBg(accent)} />
    <text x={48 + i * 150 + 69} y="338" fontSize={capsuleFontSize(t)} textAnchor="middle" fill={capsuleText(accent)}>{t}</text>
  </g>
))}
```

`FeatureBar.tsx`：`<RepoCover ... loading="eager" />`。

`Capsule.css`：

```css
.capsule {
  ...
  max-width: 16em;
  overflow: hidden;
  text-overflow: ellipsis;
  vertical-align: middle;
}
```

`base.css` 追加：

```css
/* 触摸与点按反馈（规范 §8 可达性补充） */
button, a, [role="button"], input, textarea, select {
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
}
```

数值行 tabular-nums（各 css 追加一行）：`.repo-card-meta, .search-meta, .repo-meta, .feature-meta, .profile-stats { font-variant-numeric: tabular-nums; }`（若同名类冲突按文件分别追加到对应规则块）。

标题平衡：`.feature-title, .repo-name, .preview-title, .done-title { text-wrap: balance; }`（分别落在 FeatureBar.css / RepoPage.css / SubmitPage.css）。

窄屏修补：

`RepoPage.css`（`.repo-head` 增 `flex-wrap: wrap;`）+ 追加：

```css
@media (max-width: 760px) {
  .repo-files { flex-direction: column; }
  .file-tree {
    flex: none; width: 100%;
    max-height: 240px;
    border-right: none;
    border-bottom: 1px solid var(--line_light);
  }
  .repo-head-actions { flex-wrap: wrap; }
}
```

`SearchPage.css` 追加：

```css
@media (max-width: 640px) {
  .search-card { flex-direction: column; }
  .search-cover { flex: none; width: 100%; }
}
```

`TopBar.css` 追加：

```css
@media (max-width: 760px) {
  .logo-sub, .topbar-nav { display: none; }
  .btn-submit { padding: 0 12px; font-size: 13px; }
}
```

`SubmitPage.css` 追加：

```css
@media (max-width: 860px) {
  .submit-edit { flex-direction: column; }
  .edit-preview { flex: none; width: 100%; }
}
```

- [ ] **Step 4: 运行验证通过**

Run: `npx vitest run src/components/RepoCover.test.tsx && npx vitest run`
Expected: PASS（全量）

- [ ] **Step 5: 提交**

```bash
git add -A frontend/src
git commit -m "fix(frontend): 全局润色——封面规范/长内容截断/触摸与字号/窄屏布局"
```

---

## Self-Review

**1. Spec coverage（对照审查结论逐条）**

| 审查项 | 任务 |
|---|---|
| LoginModal 键盘/焦点/滚动/overscroll | T1 |
| RepoCard 悬停重叠 + focus-within | T2 |
| TopBar 高亮/name/autocomplete/…/保留查询词 | T3 |
| SearchPage URL/错误/空 q/竞态 | T4 |
| SubmitPage 错误恢复/计数/aria-pressed/beforeunload | T5 |
| skip link/h1/翻页错误可见/错误文案 | T6 |
| RepoPage tab+file 深链/交互回滚/tree 降级/CodeView 上限/分支链接 | T7 |
| ProfilePage 头像 role/bio Esc/autoFocus/铅笔可见 | T8 |
| Pagination 窗口化+aria-current/FileTree aria-current/CategoryBar 兜底+aria-expanded+外关/Tabs 方向键 | T9 |
| RepoCover 尺寸/懒加载/长 topic/Capsule 截断/touch-action/tap-highlight/tabular-nums/text-wrap/窄屏 | T10 |

**2. Placeholder scan**：无 TBD/TODO；每个实现步骤含完整代码。

**3. Type consistency**：
- `default_branch: string` 在 T7 的 types/mockClient/RepoPage/CodeView 全链路一致
- `RepoCover` 新 prop `loading` 由 T10 定义、FeatureBar 消费，类型 `'lazy' | 'eager'`
- `Tabs` 新 prop `panelId` 可选，RepoPage/ProfilePage 消费；SearchPage 省略合法
- `Pagination.pageItems` 返回 `(number | '…')[]`，渲染处按 `'…'` 分支
- 既有测试更新项清单：TopBar placeholder（T3）、ProfilePage 头像 label（T8）、HomePage 错误文案（T6）、Pagination aria-current（T9）、SearchPage ParamProbe（T4）、RepoPage ParamProbe（T7）
