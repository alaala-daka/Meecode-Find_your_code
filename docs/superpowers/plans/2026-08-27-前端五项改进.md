# 觅码前端五项界面改进 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地已确认的设计文档《2026-08-27-觅码-前端五项改进-design.md》：①个人主页横幅延伸 ②页脚守底 ③杏色加深 ④心形/五角星图标按钮 ⑤仓库页 skillsmp 式双列布局（含 README 渲染与同类推荐）。

**Architecture:** 全部改动在 `frontend/src` 内。除仓库页采用「布局级重构」（保留现有状态机、URL 深链与乐观更新，仅重排 JSX 并新增小组件）外，其余条目都是小范围样式/结构调整。新依赖只有 `react-markdown` 与 `remark-gfm`。每个任务独立可测、以 `feat(frontend): …` 风格提交。

**Tech Stack:** React 18 + TypeScript + Vite 5 + react-router-dom 6 + zustand；测试 vitest 2 + @testing-library/react 16 + jsdom；Markdown：react-markdown + remark-gfm。

## Global Constraints

- 裸色值只允许出现在 `src/styles/theme.css`（含各组件 CSS 之外的唯一出处），组件层一律语义 token。
- Token 契约由 `src/styles/theme.test.ts` 锁定——凡新增/修改 token 必须同步更新该测试，且测试先行。
- 本次新增色值（逐字复制）：`--bg2: #F9EFDB`、`--bg3: #F4E4C2`、`--profile_banner: linear-gradient(135deg, #0A4E8A 0%, #15619E 60%, #2E77AE 100%)`、`--on_banner: #FFFDF8`、`--on_banner_dim: rgba(255, 254, 251, 0.72)`、`--like_pink: #D84870`、`--fav_yellow: #E9A63C`。
- 动效禁令：全站仅允许两处形变动画——卡片悬停封面微缩放、收藏/点赞图标弹动 `scale 1→1.2→1`（.3s）。图标悬停只允许透明度/变色过渡。
- 界面文案一律简体中文；类名沿用现有命名风格（组件名前缀的扁平 BEM）。
- 新增运行时依赖仅两个：`react-markdown`、`remark-gfm`。不得引入其他库。
- 工作区存在他人未提交的首页改动（FeaturedHero 等）——每次提交只 `git add` 本任务 **Files** 清单中列出的路径，绝不使用 `git add -A` / `git add .`。
- 测试运行方式：在 `frontend` 目录下 `npx vitest run <文件>`；全部完成后跑 `npm run typecheck` 和 `npm run test`。
- 所有步骤的 Run 命令均以 `workdir: frontend` 执行。

---

### Task 1: App 壳页脚守底（需求 ②）

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.css`
- Test: `src/App.test.tsx`

**Interfaces:**
- Consumes: 现有 `SiteFooter` 组件（类名 `.site-footer`）。
- Produces: 根容器 `<div id="app-root">` 包裹全部路由输出与页脚；后续所有页面成为其直接子元素（Tab 3 的横幅、Task 10 的仓库页都依赖此壳存在）。

- [ ] **Step 1: 写失败测试**

在 `src/App.test.tsx` 顶部补充导入：

```tsx
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'
```

在 describe 块内部追加两个用例：

```tsx
  it('app-root 存在且页脚是其最后一个子元素', () => {
    renderAt('/')
    const root = document.querySelector('#app-root')
    expect(root).not.toBeNull()
    const children = Array.from(root!.children)
    expect(children.length).toBeGreaterThan(1)
    expect(children[children.length - 1]).toHaveClass('site-footer')
  })

  it('app-shell 样式契约：纵向弹性撑满，main 占满剩余空间', () => {
    const css = readFileSync(resolve(__dirname, 'App.css'), 'utf-8')
    expect(css).toContain('#app-root { display: flex; flex-direction: column; min-height: 100dvh; }')
    expect(css).toContain('#app-root main { flex: 1; }')
  })
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/App.test.tsx`
Expected: FAIL —— 第一个用例报 `expected null not to be null`（或 querySelector 为空），第二个用例断言不包含。

- [ ] **Step 3: 实现**

`src/App.tsx` 整体替换为：

```tsx
// src/App.tsx
import { Routes, Route, Navigate } from 'react-router-dom'
import HomePage from './pages/HomePage'
import ProfilePage from './pages/ProfilePage'
import RepoPage from './pages/RepoPage'
import SearchPage from './pages/SearchPage'
import SubmitPage from './pages/SubmitPage'
import SiteFooter from './components/SiteFooter'
import './App.css'

export default function App() {
  return (
    <div id="app-root">
      <a className="skip-link" href="#main">跳转到主要内容</a>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/search" element={<SearchPage />} />
        <Route path="/repo/:id" element={<RepoPage />} />
        <Route path="/submit" element={<SubmitPage />} />
        <Route path="/user/:login" element={<ProfilePage />} />
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>
      <SiteFooter />
    </div>
  )
}
```

`src/App.css` 整体替换为：

```css
/* src/App.css —— 页面壳：app-root 守底布局（设计文档 §3.2）+ 流式内容区 */
#app-root {
  display: flex;
  flex-direction: column;
  min-height: 100dvh;
}
#app-root main { flex: 1; }

.page-shell {
  width: 100%;
  max-width: var(--shell-max);
  margin: 0 auto;
  padding: 20px var(--shell-pad) 60px;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/App.test.tsx`
Expected: PASS（既有 7 个用例也保持绿色）。

- [ ] **Step 5: 提交**

```bash
git add src/App.tsx src/App.css src/App.test.tsx
git commit -m "feat(frontend): app-root 弹性壳使页脚始终守底"
```

---

### Task 2: 杏色背景 Token 校准（需求 ④）

**Files:**
- Modify: `src/styles/theme.css`
- Test: `src/styles/theme.test.ts`

**Interfaces:**
- Produces: `--bg2: #F9EFDB`、`--bg3: #F4E4C2`。全站通过 `var()` 生效，无任何组件代码需要改动。

- [ ] **Step 1: 写失败测试**

在 `src/styles/theme.test.ts` 中把这两行：

```ts
    ['--bg2', '#FCF7EA'],
    ['--bg3', '#FAF2E0'],
```

改为：

```ts
    ['--bg2', '#F9EFDB'],
    ['--bg3', '#F4E4C2'],
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/styles/theme.test.ts`
Expected: FAIL，两条 it.each 用例因 theme.css 未改而不匹配。

- [ ] **Step 3: 实现**

`src/styles/theme.css` 把：

```css
  --bg2: #FCF7EA;
  --bg3: #FAF2E0;
```

改为：

```css
  --bg2: #F9EFDB;
  --bg3: #F4E4C2;
```

并在注释行 `/* 背景（规范 §3.1） */` 改为 `/* 背景（2026-08-27 五项改进 §3.3 校准：bg3 杏色加重、bg2 保持层次不倒挂） */`。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/styles/theme.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/styles/theme.css src/styles/theme.test.ts
git commit -m "feat(frontend): 页面底杏色加重至 #F4E4C2，次级填充随动"
```

---

### Task 3: 个人主页横幅延伸至 tab 上方（需求 ①）

**Files:**
- Modify: `src/styles/theme.css`（新增 3 个 token）
- Test: `src/styles/theme.test.ts`（同步锁定）
- Modify: `src/pages/ProfilePage.tsx`
- Modify: `src/pages/ProfilePage.css`
- Test: `src/pages/ProfilePage.test.tsx`

**Interfaces:**
- Consumes: Task 2 已就位的 bg tokens；现有 `.profile-*` 类。
- Produces: token `--profile_banner` / `--on_banner` / `--on_banner_dim`；`.profile-banner` 由装饰元素变为包含完整头部内容的容器（删除原 `-48px` 骑线结构）。

- [ ] **Step 1: 写失败测试**

`src/styles/theme.test.ts` 的 it.each 表格末尾（`--topbar_bg` 一行之后）追加：

```ts
    ['--profile_banner', 'linear-gradient(135deg, #0A4E8A 0%, #15619E 60%, #2E77AE 100%)'],
    ['--on_banner', '#FFFDF8'],
    ['--on_banner_dim', 'rgba(255, 254, 251, 0.72)'],
```

`src/pages/ProfilePage.test.tsx` 中把第一个用例 `'横幅、骑线头像、签名与数据行'` 重命名为 `'头部整体位于横幅内'`，并在其中追加断言（放在头像 role 断言之后）：

```tsx
    expect(screen.getByRole('img', { name: 'alice 的头像' }).closest('.profile-banner')).not.toBeNull()
    // 横幅不再是纯装饰：aria-hidden 应已被移除
    expect(document.querySelector('.profile-banner')!.getAttribute('aria-hidden')).toBeNull()
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/styles/theme.test.ts src/pages/ProfilePage.test.tsx`
Expected: 两文件均有 FAIL。

- [ ] **Step 3: 实现 token**

`src/styles/theme.css` 在 `--dot_texture` 行附近追加：

```css
  /* 个人页横幅（2026-08-27 五项改进 §3.1：深蓝底白字，延伸覆盖整个头部） */
  --profile_banner: linear-gradient(135deg, #0A4E8A 0%, #15619E 60%, #2E77AE 100%);
  --on_banner: #FFFDF8;
  --on_banner_dim: rgba(255, 254, 251, 0.72);
```

- [ ] **Step 4: 改造 ProfilePage 结构**

`src/pages/ProfilePage.tsx` 把主渲染分支开头（现第 88–93 行区域）：

```tsx
      <TopBar />
      <div className="profile-banner" aria-hidden="true" />
      <main id="main" className="page-shell profile-page">
        {loadError && <ErrorBanner message="主页加载失败" onRetry={() => void loadAll()} />}
        <header className="profile-head">
```

改为：

```tsx
      <TopBar />
      <div className="profile-banner">
        <header className="page-shell profile-head-wrap">
```

并注意：
1. 头像到统计行的 JSX 内容保持不变；
2. 原 `<header className="profile-head">` 变为上方的单标签后，其闭合点（原 `</header>` 之后、`<Tabs` 之前）改为 `</header></div>` 收口；
3. 在收口之后重新开启内容区：`<main id="main" className="page-shell profile-page">`，内部保留 `loadError` 提示条一行（把它从头里挪进来）：

```tsx
        {loadError && <ErrorBanner message="主页加载失败" onRetry={() => void loadAll()} />}
        <Tabs
          items={TAB_ITEMS.map((t) => ({ ...t, count: lists[t.key as 'repos' | 'favs' | 'history'].length }))}
          active={tab}
          onChange={switchTab}
          panelId="profile-tab-panel"
        />
```

最终页面骨架应为：

```tsx
<TopBar />
<div className="profile-banner">
  <header className="page-shell profile-head-wrap">
    …头像 / 昵称 / 签名 / 数据行…
  </header>
</div>
<main id="main" className="page-shell profile-page">
  {loadError && <ErrorBanner … />}
  <Tabs … />
  <div className="profile-tab-body" …>…</div>
</main>
```

- [ ] **Step 5: 重写 ProfilePage.css 相关段落**

替换 `.profile-banner` 到 `.profile-stats b` 的整段为：

```css
.profile-loading { color: var(--text3); padding: 60px 0; text-align: center; }
/* 横幅：深蓝渐变 + 点阵纹理；头部整体位于横幅内，底边贴 tab 上沿（§3.1） */
.profile-banner {
  background:
    radial-gradient(circle at 1px 1px, var(--dot_texture) 1px, transparent 1.5px),
    var(--profile_banner);
  background-size: 22px 22px, 100% 100%;
  padding-top: 28px;
  padding-bottom: 20px;
}
.profile-page { position: relative; margin-top: 24px; }
.profile-head { display: flex; gap: 20px; align-items: flex-end; }
.profile-avatar {
  width: 96px; height: 96px;
  border-radius: 50%;
  background: var(--brand_blue);
  color: var(--on_banner);
  display: flex; align-items: center; justify-content: center;
  font-size: 40px; font-weight: 500;
  border: 4px solid var(--on_banner);
  box-shadow: var(--shadow_float);
}
.profile-info { flex: 1; padding-bottom: 6px; }
.profile-name { font-family: var(--font-mono); font-size: 18px; font-weight: 600; letter-spacing: -0.01em; color: var(--on_banner); }
.profile-bio { margin-top: 4px; color: var(--on_banner_dim); font-size: 13px; display: flex; align-items: center; gap: 8px; }
.bio-edit {
  color: var(--on_banner_dim);
  padding: 2px 6px;
  border-radius: var(--radius-sm);
  opacity: 0;
  transition: opacity var(--time-fast), color var(--time-fast);
}
.profile-bio:hover .bio-edit,
.profile-bio:focus-within .bio-edit,
.bio-edit:focus-visible { opacity: 1; }
.bio-edit:hover { color: var(--on_banner); }
.bio-input {
  margin-top: 4px;
  padding: 6px 10px;
  border: 1px solid var(--brand_blue);
  border-radius: var(--radius-md);
  font-size: 13px;
  background: var(--bg1);
  color: var(--text1);
  box-shadow: var(--focus_ring);
  outline: none;
}
.profile-stats { display: flex; gap: 20px; margin-top: 8px; color: var(--on_banner_dim); font-size: 13px; font-variant-numeric: tabular-nums; }
.profile-stats b { font-family: var(--font-mono); color: var(--on_banner); font-weight: 600; }
```

`.profile-page .tabs` 吸顶行保持不变（已是 `background: var(--bg3)`，Task 2 后自动换新底色）。删除一切 `margin-top: -48px` 引用。

- [ ] **Step 6: 运行确认通过**

Run: `npx vitest run src/pages/ProfilePage.test.tsx src/styles/theme.test.ts`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add src/styles/theme.css src/styles/theme.test.ts src/pages/ProfilePage.tsx src/pages/ProfilePage.css src/pages/ProfilePage.test.tsx
git commit -m "feat(frontend): 个人页头部整体纳入深蓝横幅直至分类行上方"
```

---

### Task 4: IconAction 图标按钮 + favorites_count 数据链路（需求 ⑤）

**Files:**
- Create: `src/components/IconAction.tsx`
- Create: `src/components/IconAction.css`
- Modify: `src/api/types.ts`
- Modify: `src/api/fixtures.ts`
- Modify: `src/api/mockClient.ts`
- Modify: `src/styles/theme.css`（新增 2 个 token）
- Test: `src/components/IconAction.test.tsx`
- Test: `src/api/mockClient.test.ts`
- Test: `src/styles/theme.test.ts`

**Interfaces:**
- Produces: `export default function IconAction(props: { kind: 'like' | 'favorite'; on: boolean; count?: number; busy?: boolean; onClick(): void }): JSX.Element`（Task 10 消费）；`RepoCardData.favorites_count?: number`（Task 10 展示计数）；token `--like_pink`、`--fav_yellow`。

- [ ] **Step 1: 写失败测试（token 契约）**

`src/styles/theme.test.ts` it.each 表末尾追加：

```ts
    ['--like_pink', '#D84870'],
    ['--fav_yellow', '#E9A63C'],
```

- [ ] **Step 2: 写失败测试（组件行为）**

新建 `src/components/IconAction.test.tsx`：

```tsx
// src/components/IconAction.test.tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import IconAction from './IconAction'

describe('IconAction', () => {
  it('未点亮：空心、aria-pressed=false、无计数', () => {
    render(<IconAction kind="like" on={false} onClick={() => {}} />)
    const btn = screen.getByRole('button', { name: '点赞' })
    expect(btn).toHaveAttribute('aria-pressed', 'false')
    expect(btn.querySelector('.icon-action-count')).toBeNull()
    const path = btn.querySelector('svg path')!
    expect(path.getAttribute('fill')).toBe('none')
  })

  it('点亮：aria-pressed=true 且显示计数值', () => {
    render(<IconAction kind="favorite" on count={67} onClick={() => {}} />)
    const btn = screen.getByRole('button', { name: '收藏' })
    expect(btn).toHaveAttribute('aria-pressed', 'true')
    expect(screen.getByText('67')).toBeInTheDocument()
  })

  it('点击回调触发一次', async () => {
    const spy = vi.fn()
    render(<IconAction kind="like" on={false} count={23} onClick={spy} />)
    await userEvent.click(screen.getByRole('button', { name: '点赞' }))
    expect(spy).toHaveBeenCalledTimes(1)
  })

  it('busy 时禁用防连点', () => {
    render(<IconAction kind="favorite" on={false} busy onClick={() => {}} />)
    expect(screen.getByRole('button', { name: '收藏' })).toBeDisabled()
  })

  it('count 为 undefined 时不渲染数字（stars 字段后端未供数场景）', () => {
    render(<IconAction kind="favorite" on={false} onClick={() => {}} />)
    expect(document.querySelectorAll('.icon-action-count')).toHaveLength(0)
  })
})
```

- [ ] **Step 3: 写失败测试（mock 计数联动）**

`src/api/mockClient.test.ts` 追加用例：

```tsx
  it('interact 收藏切换 favorites_count 计数', async () => {
    const api = createMockClient()
    const before = (await api.repo(2)).favorites_count!
    await api.interact(2, 'favorite', true)
    expect((await api.repo(2)).favorites_count).toBe(before + 1)
    await api.interact(2, 'favorite', false)
    expect((await api.repo(2)).favorites_count).toBe(before)
  })
```

- [ ] **Step 4: 运行确认失败**

Run: `npx vitest run src/styles/theme.test.ts src/components/IconAction.test.tsx src/api/mockClient.test.ts`
Expected: 三处 FAIL（token 缺失 / 模块不存在 / favorites_count undefined 报错）。

- [ ] **Step 5: 实现 token + DTO + mock + fixtures**

`src/styles/theme.css` 在暖琥珀段之后追加：

```css
  /* 点赞粉 / 收藏金（2026-08-27 五项改进 §3.4） */
  --like_pink: #D84870;
  --fav_yellow: #E9A63C;
```

`src/api/types.ts` 在 `cover_url: string | null` 之后加一行字段：

```ts
  favorites_count?: number   // 每仓收藏数；真实后端就绪前后端可缺省，UI 不显数字
```

`src/api/fixtures.ts` 给 12 条 fixture 各加 `favorites_count`（依次加到每行对象末尾、`cover_url: null,` 之后）：id1 `24`、id2 `5`、id3 `40`、id4 `9`、id5 `3`、id6 `22`、id7 `6`、id8 `15`、id9 `7`、id10 `18`、id11 `2`、id12 `10`。例如 id1 行改为：

```ts
  { id: 1, full_name: 'alice/mini-agent', title: 'mini-agent', owner_login: 'alice', language: 'Python', topics: ['agent', 'llm', 'runtime'], stars: 128, views: 3100, likes: 45, favorites_count: 24, source: 'submitted', category: 'AI 与机器学习', tagline_zh: '给 LLM Agent 的最小运行时，200 行可读完', published_at: iso(1), cover_url: null },
```

`src/api/mockClient.ts` 的 `submitRepo` 新建对象中加入 `favorites_count: 0,`（`likes: 0,` 同处）；`interact` 改为：

```ts
    async interact(repoId, kind: InteractKind, on) {
      const set = kind === 'like' ? state.likes : state.favorites
      if (on) set.add(repoId); else set.delete(repoId)
      const card = state.repos.find((r) => r.id === repoId)
      if (card && kind === 'like') card.likes += on ? 1 : -1
      if (card && kind === 'favorite' && card.favorites_count !== undefined) card.favorites_count += on ? 1 : -1
    },
```

- [ ] **Step 6: 实现 IconAction 组件**

新建 `src/components/IconAction.tsx`：

```tsx
// src/components/IconAction.tsx —— 2026-08-27 五项改进 §3.4：心形点赞 / 五角星收藏
import './IconAction.css'

const GLYPH = {
  like: 'M12 21s-7.5-4.9-10-9.3C.6 8.2 2.6 4.5 6.2 4.1c2-.2 3.9.8 5 2.4h1.6c1.1-1.6 3-2.6 5-2.4 3.6.4 5.6 4.1 4.2 7.6C19.5 16.1 12 21 12 21z',
  favorite: 'M12 1.9l3.35 6.06 6.55 1.02-4.78 4.87.96 6.68L12 17.36l-6.08 3.17.96-6.68L2.1 8.98l6.55-1.02z',
} as const

const LABEL = { like: '点赞', favorite: '收藏' } as const

interface Props {
  kind: 'like' | 'favorite'
  on: boolean
  /** undefined/null 不渲染数字（favorites_count 后端就绪前的降级） */
  count?: number
  busy?: boolean
  onClick(): void
}

export default function IconAction({ kind, on, count, busy = false, onClick }: Props) {
  const size = kind === 'favorite' ? 29 : 26 // 五角星等比放大，上下顶缘与心形对齐（决策 #5）
  return (
    <button
      type="button"
      className={`icon-action kind-${kind}${on ? ' is-on' : ''}${busy ? ' is-busy' : ''}`}
      aria-label={LABEL[kind]}
      aria-pressed={on}
      disabled={busy}
      onClick={onClick}
    >
      {/* key=on：切换时重建 svg，让点亮弹动动画每次都会重播 */}
      <svg key={on ? 'on' : 'off'} className="icon-action-glyph" viewBox="0 0 24 24" width={size} height={size} aria-hidden="true">
        <path d={GLYPH[kind]} strokeWidth={2.2} strokeLinejoin="round" />
      </svg>
      {count !== undefined && <span className="icon-action-count">{count}</span>}
    </button>
  )
}
```

新建 `src/components/IconAction.css`：

```css
/* src/components/IconAction.css —— 未点亮=中空粗描边，点亮=实心弹动一次 */
.icon-action {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  padding: 4px 2px;
  border-radius: var(--radius-md);
  color: var(--text1);
  transition: opacity var(--time-fast);
}
.icon-action:hover:not(:disabled) { opacity: 0.85; }
.icon-action.is-busy { opacity: 0.55; }

.icon-action .icon-action-glyph { display: block; }
.icon-action .icon-action-glyph path { fill: none; stroke: currentColor; }

@keyframes icon-pop { 0% { transform: scale(1); } 50% { transform: scale(1.2); } 100% { transform: scale(1); } }
.icon-action.is-on .icon-action-glyph { animation: icon-pop var(--time-slow) var(--ease); }
.icon-action.is-on.kind-like .icon-action-glyph path { fill: var(--like_pink); stroke: none; }
.icon-action.is-on.kind-favorite .icon-action-glyph path { fill: var(--fav_yellow); stroke: none; }

.icon-action-count { font-size: 13px; font-weight: 500; font-variant-numeric: tabular-nums; }
```

说明：动画时长/缓动走既有 token；`prefers-reduced-motion` 由 `base.css` 的全局规则兜底归零，无需在此重复。

- [ ] **Step 7: 运行确认通过**

Run: `npx vitest run src/styles/theme.test.ts src/components/IconAction.test.tsx src/api/mockClient.test.ts`
Expected: PASS。

- [ ] **Step 8: 提交**

```bash
git add src/components/IconAction.tsx src/components/IconAction.css src/components/IconAction.test.tsx src/api/types.ts src/api/fixtures.ts src/api/mockClient.ts src/api/mockClient.test.ts src/styles/theme.css src/styles/theme.test.ts
git commit -m "feat(frontend): 心形/五角星图标按钮与 favorites_count 数据链路"
```

---

### Task 5: related API（同分类推荐数据源）

**Files:**
- Modify: `src/api/types.ts`
- Modify: `src/api/mockClient.ts`
- Test: `src/api/mockClient.test.ts`

**Interfaces:**
- Consumes: `RepoCardData[]`。
- Produces: ApiClient 接口新增 `related(repoId: number): Promise<RepoCardData[]>` —— 同分类、排除自身、按 stars 降序、最多 3 条；仓库不存在时 reject；Task 9 的 RepoRail 消费。

- [ ] **Step 1: 写失败测试**

`src/api/mockClient.test.ts` 追加：

```tsx
  it('related：同分类 stars 降序、排除自身、至多 3 条', async () => {
    const api = createMockClient()
    // fixture：AI 与机器学习 只有 1(128★) 与 6(210★)，排除自身(1)剩 llm-eval-kit
    const rel = await api.related(1)
    expect(rel.map((r) => r.id)).toEqual([6])
  })
  it('related：同分类仅自身时返回空数组（严格不跨类补位，决策 #7）', async () => {
    const api = createMockClient()
    expect(await api.related(11)).toEqual([]) // 其他 分类仅 pixel-sort 自己
  })
  it('related：仓库不存在 reject', async () => {
    const api = createMockClient()
    await expect(api.related(999)).rejects.toThrow('仓库不存在')
  })
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/api/mockClient.test.ts`
Expected: FAIL，`api.related is not a function`。

- [ ] **Step 3: 实现**

`src/api/types.ts` 的 `ApiClient` 接口中，`repoFile(...)` 一行之后插入：

```ts
  related(repoId: number): Promise<RepoCardData[]>
```

`src/api/mockClient.ts` 的 `repoFile` 方法之后插入：

```ts
    async related(repoId): Promise<RepoCardData[]> {
      const self = state.repos.find((r) => r.id === repoId)
      if (!self) throw new Error('仓库不存在')
      return state.repos
        .filter((r) => r.category === self.category && r.id !== repoId)
        .sort((a, b) => b.stars - a.stars)
        .slice(0, 3)
    },
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/api/mockClient.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/api/types.ts src/api/mockClient.ts src/api/mockClient.test.ts
git commit -m "feat(frontend): related API 同分类高星推荐（≤3 条、不跨类补位）"
```

---

### Task 6: AuthorCard 作者卡

**Files:**
- Create: `src/components/AuthorCard.tsx`
- Create: `src/components/AuthorCard.css`
- Test: `src/components/AuthorCard.test.tsx`

**Interfaces:**
- Produces: `AuthorCard({ ownerLogin, githubUrl })`；结构约定——根 `<section class="author-card" aria-label="仓库作者">`，跳转链接 accessible name 为「跳转 GitHub ↗」，站内入口为「浏览创作者其他仓库」。Task 9 消费。

- [ ] **Step 1: 写失败测试**

新建 `src/components/AuthorCard.test.tsx`：

```tsx
// src/components/AuthorCard.test.tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { describe, expect, it } from 'vitest'
import AuthorCard from './AuthorCard'

describe('AuthorCard', () => {
  it('展示首字母头像、名称、GitHub 外链与站内创作者入口', () => {
    render(
      <MemoryRouter>
        <AuthorCard ownerLogin="alice" githubUrl="https://github.com/alice/mini-agent" />
      </MemoryRouter>,
    )
    expect(screen.getByRole('img', { name: 'alice 的头像' })).toHaveTextContent('A')
    expect(screen.getByText('alice')).toBeInTheDocument()
    const gh = screen.getByRole('link', { name: '跳转 GitHub ↗' })
    expect(gh).toHaveAttribute('href', 'https://github.com/alice/mini-agent')
    expect(gh).toHaveAttribute('target', '_blank')
    expect(screen.getByRole('link', { name: '浏览创作者其他仓库' })).toHaveAttribute('href', '/user/alice')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/AuthorCard.test.tsx`
Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现**

新建 `src/components/AuthorCard.tsx`：

```tsx
// src/components/AuthorCard.tsx —— 五项改进 §3.5 右栏作者卡
import { Link } from 'react-router-dom'
import './AuthorCard.css'

interface Props {
  ownerLogin: string
  githubUrl: string
}

export default function AuthorCard({ ownerLogin, githubUrl }: Props) {
  return (
    <section className="author-card" aria-label="仓库作者">
      <div className="author-card-id">
        <span className="author-card-avatar" role="img" aria-label={`${ownerLogin} 的头像`}>
          {ownerLogin.slice(0, 1).toUpperCase()}
        </span>
        <span className="author-card-name">{ownerLogin}</span>
      </div>
      <a className="btn btn-primary author-card-btn" href={githubUrl} target="_blank" rel="noreferrer">跳转 GitHub ↗</a>
      <Link className="btn btn-ghost author-card-btn" to={`/user/${ownerLogin}`}>浏览创作者其他仓库</Link>
    </section>
  )
}
```

新建 `src/components/AuthorCard.css`：

```css
/* src/components/AuthorCard.css */
.author-card {
  background: var(--bg1);
  border-radius: var(--radius-xl);
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.author-card-id { display: flex; align-items: center; gap: 10px; }
.author-card-avatar {
  width: 44px; height: 44px;
  border-radius: 50%;
  background: var(--brand_blue);
  color: var(--bg1);
  display: flex; align-items: center; justify-content: center;
  font-weight: 600; font-size: 18px;
}
.author-card-name { font-family: var(--font-mono); font-weight: 600; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.author-card-btn { width: 100%; justify-content: center; }

/* 窄屏单列堆叠时作者卡转为横向布局（§3.5 断点行为） */
@media (max-width: 1100px) {
  .author-card { flex-direction: row; flex-wrap: wrap; align-items: center; }
  .author-card-id { flex: 1 1 auto; }
  .author-card-btn { width: auto; flex: 0 1 auto; }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/components/AuthorCard.test.tsx`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/components/AuthorCard.tsx src/components/AuthorCard.css src/components/AuthorCard.test.tsx
git commit -m "feat(frontend): 右栏作者卡（GitHub 外链 + 创作者站内入口）"
```

---

### Task 7: RelatedList 同类推荐列表

**Files:**
- Create: `src/components/RelatedList.tsx`
- Create: `src/components/RelatedList.css`
- Test: `src/components/RelatedList.test.tsx`

**Interfaces:**
- Consumes: `RepoCardData[]`（来自 Task 5 的 `related()` 结果）、复用 `RepoCard`。
- Produces: `RelatedList({ items }: { items: RepoCardData[] })`；结构约定——根 `<section class="related-list" aria-label="同类推荐">`，标题「同类推荐」，items 为空时渲染 `.related-list-empty` 文案「同分类暂无更多仓库」。（加载失败整块隐藏的职责在 Task 9 的父级。）

- [ ] **Step 1: 写失败测试**

新建 `src/components/RelatedList.test.tsx`：

```tsx
// src/components/RelatedList.test.tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { describe, expect, it } from 'vitest'
import { FIXTURE_REPOS } from '../api/fixtures'
import RelatedList from './RelatedList'

function wrap(ui: React.ReactElement) {
  return render(<MemoryRouter>{ui}</MemoryRouter>)
}

describe('RelatedList', () => {
  it('渲染若干 RepoCard 卡片', () => {
    wrap(<RelatedList items={[FIXTURE_REPOS[5], FIXTURE_REPOS[1]]} />)
    expect(screen.getByLabelText('同类推荐').querySelectorAll('.repo-card')).toHaveLength(2)
  })

  it('空数组显示「同分类暂无更多仓库」，不渲染卡片', () => {
    wrap(<RelatedList items={[]} />)
    expect(screen.getByText('同分类暂无更多仓库')).toBeInTheDocument()
    expect(document.querySelectorAll('.repo-card')).toHaveLength(0)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/RelatedList.test.tsx`
Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现**

新建 `src/components/RelatedList.tsx`：

```tsx
// src/components/RelatedList.tsx —— 五项改进 §3.5 同类推荐（复用 RepoCard，纵列 ≤3）
import type { RepoCardData } from '../api/types'
import RepoCard from './RepoCard'
import './RelatedList.css'

interface Props {
  items: RepoCardData[]
}

export default function RelatedList({ items }: Props) {
  return (
    <section className="related-list" aria-label="同类推荐">
      <h2 className="related-list-title">同类推荐</h2>
      {items.length === 0 ? (
        <p className="related-list-empty">同分类暂无更多仓库</p>
      ) : (
        <ul className="related-list-cards">
          {items.map((r) => (
            <li key={r.id}><RepoCard data={r} /></li>
          ))}
        </ul>
      )}
    </section>
  )
}
```

新建 `src/components/RelatedList.css`：

```css
/* src/components/RelatedList.css */
.related-list-title { font-size: 13px; font-weight: 500; color: var(--text2); letter-spacing: 0.02em; margin-bottom: 12px; }
.related-list-cards { display: flex; flex-direction: column; gap: 16px; }
.related-list-empty {
  background: var(--bg1);
  border-radius: var(--radius-lg);
  padding: 24px 16px;
  text-align: center;
  color: var(--text3);
  font-size: 13px;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/components/RelatedList.test.tsx`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/components/RelatedList.tsx src/components/RelatedList.css src/components/RelatedList.test.tsx
git commit -m "feat(frontend): 同类推荐列表组件（空态文案与卡片纵列）"
```

---

### Task 8: ReadmeSection 自述文件（安装 Markdown 依赖）

**Files:**
- Create: `src/components/ReadmeSection.tsx`
- Create: `src/components/ReadmeSection.css`
- Test: `src/components/ReadmeSection.test.tsx`
- Modify: `package.json` / `package-lock.json`（经 npm install 自动变更）

**Interfaces:**
- Consumes: `api.repoFile(repoId, path)`（现有方法）、`RepoTreeItem[]`。
- Produces: `ReadmeSection({ repoId, tree }: { repoId: number; tree: RepoTreeItem[] })`；结构约定——根 `<section class="readme-section" aria-label="自述文件">`，标题「自述文件」，缺失/失败渲染文案「暂无自述」。Task 9/10 消费。

- [ ] **Step 1: 安装依赖**

Run: `npm i react-markdown remark-gfm`
Expected: 两者进入 dependencies；版本 react-markdown ^9.x、remark-gfm ^4.x 或更新兼容线。

- [ ] **Step 2: 写失败测试**

新建 `src/components/ReadmeSection.test.tsx`：

```tsx
// src/components/ReadmeSection.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it, vi, afterEach } from 'vitest'
import type { RepoTreeItem } from '../api/types'
import ReadmeSection from './ReadmeSection'

const TREE_WITH_README: RepoTreeItem[] = [
  { name: 'README.md', path: 'README.md', type: 'file' },
  { name: 'src', path: 'src', type: 'dir', children: [] },
]

describe('ReadmeSection', () => {
  afterEach(() => vi.restoreAllMocks())

  it('定位 README.md 并完整渲染 Markdown（GFM 标题）', async () => {
    render(<ReadmeSection repoId={1} tree={TREE_WITH_README} />)
    expect(await screen.findByRole('heading', { level: 1, name: 'mini-agent' })).toBeInTheDocument()
    expect(screen.getByText(/200 行的 LLM Agent 运行时/)).toBeInTheDocument()
  })

  it('树中没有 README：立即显示「暂无自述」且不发请求', async () => {
    const { api } = await import('../api/client')
    const spy = vi.spyOn(api, 'repoFile')
    render(<ReadmeSection repoId={1} tree={[{ name: 'main.py', path: 'main.py', type: 'file' }]} />)
    expect(screen.getByText('暂无自述')).toBeInTheDocument()
    expect(spy).not.toHaveBeenCalled()
  })

  it('读取失败同样降级为「暂无自述」', async () => {
    const { api } = await import('../api/client')
    vi.spyOn(api, 'repoFile').mockRejectedValueOnce(new Error('boom'))
    render(<ReadmeSection repoId={1} tree={TREE_WITH_README} />)
    expect(await screen.findByText('暂无自述')).toBeInTheDocument()
  })
})
```

- [ ] **Step 3: 运行确认失败**

Run: `npx vitest run src/components/ReadmeSection.test.tsx`
Expected: FAIL，模块不存在。

- [ ] **Step 4: 实现**

新建 `src/components/ReadmeSection.tsx`：

```tsx
// src/components/ReadmeSection.tsx —— 五项改进 §3.5：自述文件（Markdown 完整渲染）
import { useEffect, useRef, useState } from 'react'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import type { Components } from 'react-markdown'
import type { RepoTreeItem } from '../api/types'
import { api } from '../api/client'
import './ReadmeSection.css'

const README_RE = /^readme(\.(md|markdown|txt))?$/i

function findReadme(tree: RepoTreeItem[]): string | null {
  const hit = tree.find((n) => n.type === 'file' && README_RE.test(n.name))
  return hit?.path ?? null
}

/* 安全基线：不开 rehype-raw，原始 HTML 不会执行（决策 #9）；
   外链新窗口打开，相对路径资源不可达则退化为文本/占位符 */
const markdownComponents: Components = {
  a({ href, children }) {
    if (!href || !/^https?:\/\//.test(href)) return <span>{children}</span>
    return <a href={href} target="_blank" rel="noreferrer">{children}</a>
  },
  img({ alt, src }) {
    const url = typeof src === 'string' ? src : undefined
    if (!url || !/^https?:\/\//.test(url)) return <span>{alt || '[图片]'}</span>
    return <img src={url} alt={alt ?? ''} loading="lazy" />
  },
}

type Phase = 'loading' | 'ready' | 'missing'

interface Props {
  repoId: number
  tree: RepoTreeItem[]
}

export default function ReadmeSection({ repoId, tree }: Props) {
  const [phase, setPhase] = useState<Phase>('loading')
  const [content, setContent] = useState('')
  const reqRef = useRef(0)

  useEffect(() => {
    const path = findReadme(tree)
    if (!path) { setPhase('missing'); return }
    const req = ++reqRef.current
    setPhase('loading')
    api.repoFile(repoId, path)
      .then((f) => {
        if (req !== reqRef.current) return
        setContent(f.content)
        setPhase('ready')
      })
      .catch(() => {
        if (req === reqRef.current) setPhase('missing')
      })
  }, [repoId, tree])

  return (
    <section className="readme-section" aria-label="自述文件">
      <h2 className="readme-section-title">自述文件</h2>
      {phase === 'loading' && null}
      {phase === 'missing' && <p className="readme-section-empty">暂无自述</p>}
      {phase === 'ready' && (
        <div className="readme-section-body">
          <ReactMarkdown remarkPlugins={[remarkGfm]} components={markdownComponents}>{content}</ReactMarkdown>
        </div>
      )}
    </section>
  )
}
```

新建 `src/components/ReadmeSection.css`：

```css
/* src/components/ReadmeSection.css —— GitHub 风格克制排版，全部引用 token */
.readme-section {
  background: var(--bg1);
  border-radius: var(--radius-xl);
  padding: 24px 28px;
  margin-top: 24px;
}
.readme-section-title {
  font-size: 16px;
  font-weight: 500;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--line_light);
  margin-bottom: 16px;
}
.readme-section-empty { text-align: center; color: var(--text3); padding: 56px 0; }
.readme-section-body > *:first-child { margin-top: 0; }
.readme-section-body h1, .readme-section-body h2, .readme-section-body h3,
.readme-section-body h4 { font-family: var(--font-mono); line-height: 1.35; margin: 20px 0 8px; }
.readme-section-body h1 { font-size: 20px; }
.readme-section-body h2 { font-size: 17px; }
.readme-section-body h3 { font-size: 15px; }
.readme-section-body h4 { font-size: 14px; }
.readme-section-body p { margin: 8px 0; }
.readme-section-body ul { list-style: disc; padding-left: 22px; margin: 8px 0; }
.readme-section-body ol { list-style: decimal; padding-left: 22px; margin: 8px 0; }
.readme-section-body li { margin: 2px 0; }
.readme-section-body a { color: var(--brand_blue); }
.readme-section-body a:hover { color: var(--brand_blue_hover); }
.readme-section-body :not(pre) > code {
  font-family: var(--font-mono);
  font-size: 13px;
  background: var(--bg2);
  border-radius: var(--radius-sm);
  padding: 2px 5px;
}
.readme-section-body pre {
  background: var(--bg2);
  border-radius: var(--radius-lg);
  padding: 14px 16px;
  overflow: auto;
  margin: 12px 0;
  line-height: 1.6;
}
.readme-section-body pre code { font-family: var(--font-mono); font-size: 13px; }
.readme-section-body blockquote {
  border-left: 3px solid var(--line_bold);
  color: var(--text2);
  padding-left: 12px;
  margin: 12px 0;
}
.readme-section-body table { border-collapse: collapse; margin: 12px 0; }
.readme-section-body th, .readme-section-body td { border: 1px solid var(--line_regular); padding: 6px 10px; }
.readme-section-body th { background: var(--bg2); }
.readme-section-body img { max-width: 100%; border-radius: var(--radius-lg); }
.readme-section-body hr { border: 0; border-top: 1px solid var(--line_light); margin: 16px 0; }
```

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run src/components/ReadmeSection.test.tsx`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add package.json package-lock.json src/components/ReadmeSection.tsx src/components/ReadmeSection.css src/components/ReadmeSection.test.tsx
git commit -m "feat(frontend): 自述文件区块（react-markdown 渲染 + 暂无自述降级）"
```

---

### Task 9: RepoRail 右栏（作者卡 + 同类推荐，失败静默隐藏）

**Files:**
- Create: `src/components/RepoRail.tsx`
- Create: `src/components/RepoRail.css`
- Test: `src/components/RepoRail.test.tsx`

**Interfaces:**
- Consumes: Task 5 `api.related`、Task 6 `AuthorCard`、Task 7 `RelatedList`、`RepoDetail` 类型。
- Produces: `RepoRail({ repo }: { repo: RepoDetail })`；结构约定——根 `<aside class="repo-rail">`，内层 `.repo-rail-sticky`；作者卡恒在（`aria-label="仓库作者"`）；`related` 成功才渲染推荐区（`aria-label="同类推荐"`），失败/加载中整块不出现。Task 10 消费。

- [ ] **Step 1: 写失败测试**

新建 `src/components/RepoRail.test.tsx`：

```tsx
// src/components/RepoRail.test.tsx
import { render, screen, waitFor } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { toDetail } from '../api/testUtils'
import RepoRail from './RepoRail'

function renderRail(id: number) {
  return render(
    <MemoryRouter initialEntries={[`/repo/${id}`]}>
      <RepoRail repo={toDetail(id)} />
    </MemoryRouter>,
  )
}

describe('RepoRail', () => {
  afterEach(() => vi.restoreAllMocks())

  it('作者卡常驻；成功时渲染同类推荐卡片', async () => {
    renderRail(1)
    expect(screen.getByLabelText('仓库作者')).toBeInTheDocument()
    const rail = await screen.findByLabelText('同类推荐')
    // fixture：AI 与机器学习 排除自身只剩 llm-eval-kit
    expect(rail.querySelectorAll('.repo-card')).toHaveLength(1)
  })

  it('加载中不闪出推荐区', () => {
    renderRail(1)
    // 同步阶段：related 尚未 resolve
    expect(screen.queryByLabelText('同类推荐')).not.toBeInTheDocument()
  })

  it('related 失败：推荐区静默隐藏，作者卡仍在（§3.5 错误策略）', async () => {
    const { api } = await import('../api/client')
    vi.spyOn(api, 'related').mockRejectedValue(new Error('boom'))
    renderRail(1)
    await waitFor(() => expect(screen.getByLabelText('仓库作者')).toBeInTheDocument())
    expect(screen.queryByLabelText('同类推荐')).not.toBeInTheDocument()
  })
})
```

注意：测试需要一个把 `RepoCardData` 升维成 `RepoDetail` 的现有帮助。若 `src/api/testUtils.ts` 不存在则先创建：

```ts
// src/api/testUtils.ts —— 测试辅助：card → detail（与 mockClient.toDetail 同规则）
import type { RepoCardData, RepoDetail } from './types'
import { FIXTURE_REPOS } from './fixtures'

export function toDetail(id: number): RepoDetail {
  const card: RepoCardData = FIXTURE_REPOS.find((r) => r.id === id)!
  if (!card) throw new Error(`fixture 缺少 id=${id}`)
  return {
    ...card,
    intro_zh: `${card.tagline_zh}。这里放详细介绍，介绍作者在投稿时可以自由编辑。`,
    github_url: `https://github.com/${card.full_name}`,
    default_branch: 'main',
    discussions_open: card.id % 2 === 1,
  }
}
```

同时在 `src/api/mocks` 无关前提下（可选重构）不动 mockClient 私有函数——重复一份供测试稳定即可。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/RepoRail.test.tsx`
Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现**

新建 `src/components/RepoRail.tsx`：

```tsx
// src/components/RepoRail.tsx —— §3.5 右栏：作者卡 + 同类推荐；推荐失败静默隐藏
import { useEffect, useState } from 'react'
import type { RepoCardData, RepoDetail } from '../api/types'
import { api } from '../api/client'
import AuthorCard from './AuthorCard'
import RelatedList from './RelatedList'
import './RepoRail.css'

type RailPhase =
  | { state: 'loading'; items: [] }
  | { state: 'ok'; items: RepoCardData[] }
  | { state: 'hidden'; items: [] }

export default function RepoRail({ repo }: { repo: RepoDetail }) {
  const [rail, setRail] = useState<RailPhase>({ state: 'loading', items: [] })

  useEffect(() => {
    let alive = true
    setRail({ state: 'loading', items: [] })
    api.related(repo.id)
      .then((items) => { if (alive) setRail({ state: 'ok', items }) })
      .catch(() => { if (alive) setRail({ state: 'hidden', items: [] }) })
    return () => { alive = false }
  }, [repo.id])

  return (
    <aside className="repo-rail">
      <div className="repo-rail-sticky">
        <AuthorCard ownerLogin={repo.owner_login} githubUrl={repo.github_url} />
        {rail.state === 'ok' && <RelatedList items={rail.items} />}
      </div>
    </aside>
  )
}
```

新建 `src/components/RepoRail.css`：

```css
/* src/components/RepoRail.css —— 左滚右停：右栏内容吸顶固定（§3.5） */
.repo-rail { min-width: 0; }
.repo-rail-sticky {
  position: sticky;
  top: 80px;
  align-self: start;
  display: flex;
  flex-direction: column;
  gap: 20px;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/components/RepoRail.test.tsx`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/api/testUtils.ts src/components/RepoRail.tsx src/components/RepoRail.css src/components/RepoRail.test.tsx
git commit -m "feat(frontend): 仓库右栏容器（吸顶、推荐失败静默隐藏）"
```

---

### Task 10: RepoPage 双列集成（需求 ③ 主菜）

**Files:**
- Modify: `src/pages/RepoPage.tsx`
- Modify: `src/pages/RepoPage.css`
- Test: `src/pages/RepoPage.test.tsx`

**Interfaces:**
- Consumes: Task 4 `IconAction`、Task 8 `ReadmeSection`、Task 9 `RepoRail`、既有 `Tabs/FileTree/CodeView/Capsule/LoginModal/EmptyState`。
- Produces: 仓库页新骨架——`.repo-grid` 双列（左列滚动内容、右列 sticky rail），元信息行右端为两枚图标按钮，tab 逻辑与 URL 深链不变。

- [ ] **Step 1: 更新既有测试以匹配新形态（写失败测试）**

对 `src/pages/RepoPage.test.tsx`：

1. 第一个用例中的两条断言：

```tsx
    const gh = screen.getByText('去 GitHub ↗')
    expect(gh.closest('a')).toHaveAttribute('href', 'https://github.com/alice/mini-agent')
```

改为：

```tsx
    const gh = screen.getByRole('link', { name: '跳转 GitHub ↗' })
    expect(gh).toHaveAttribute('href', 'https://github.com/alice/mini-agent')
```

2. 两个点赞用例无需改动（`is-on` 类与 `aria-label="点赞"` 名义在 IconAction 上保持一致）。

3. 文件末尾追加新用例：

```tsx
  it('双列骨架：右栏作者卡常驻，同类推荐来自同分类', async () => {
    renderAt('/repo/1')
    await screen.findByText('mini-agent')
    expect(screen.getByRole('link', { name: '浏览创作者其他仓库' })).toHaveAttribute('href', '/user/alice')
    const rail = await screen.findByLabelText('同类推荐')
    expect(rail.querySelectorAll('.repo-card')).toHaveLength(1)
  })

  it('元信息行带图标计数按钮：心形=likes 数，金星=favorites_count 数', async () => {
    renderAt('/repo/1')
    await screen.findByText('mini-agent')
    expect(screen.getByRole('button', { name: '点赞' })).toHaveTextContent('45')
    expect(screen.getByRole('button', { name: '收藏' })).toHaveTextContent('24')
  })

  it('已登录收藏成功后收藏数即时 +1（可选字段存在时）', async () => {
    useAuthStore.setState({ user: FIXTURE_USER })
    renderAt('/repo/2')
    await screen.findByText('tinyfetch')
    await userEvent.click(screen.getByRole('button', { name: '收藏' }))
    await waitFor(() => expect(screen.getByRole('button', { name: '收藏' })).toHaveTextContent('6'))
  })

  it('自述文件在文件预览之后、讨论区之前', async () => {
    const { container } = renderAt('/repo/1')
    await screen.findAllByText('README.md')
    const readme = container.querySelector('.readme-section')
    const discuss = container.querySelector('.repo-discussions')
    expect(readme).toBeTruthy()
    expect(discuss).toBeTruthy()
    expect(readme!.compareDocumentPosition(discuss!) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy()
  })

  it('?tab=explain 深链直达解读占位', async () => {
    renderAt('/repo/1?tab=explain')
    expect(await screen.findByText('解读功能建设中')).toBeInTheDocument()
  })
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/RepoPage.test.tsx`
Expected: 信息头用例 FAIL（找不到「跳转 GitHub ↗」），新增五个用例 FAIL。

- [ ] **Step 3: 改造 RepoPage.tsx**

a) 导入调整——删除 `Button` 导入，新增：

```tsx
import IconAction from '../components/IconAction'
import ReadmeSection from '../components/ReadmeSection'
import RepoRail from '../components/RepoRail'
```

b) `toggleFav` 的 try 内、`await api.interact(...)` 之后增加乐观计数（仅在可选字段存在时）：

```ts
      setDetail((d) => (d && d.favorites_count != null ? { ...d, favorites_count: d.favorites_count + (next ? 1 : -1) } : d))
```

c) 主渲染分支（`if (!detail)` 兜底之后的 return）整体重排为：

```tsx
  return (
    <>
      <TopBar />
      <main id="main" className="page-shell repo-page">
        <div className="repo-grid">
          <div className="repo-main">
            <section className="repo-head" style={{ borderTop: `3px solid ${languageColor(detail.language)}` }}>
              <h1 className="repo-name">{detail.title}</h1>
              <p className="repo-tagline">{detail.tagline_zh}</p>
              <p className="repo-meta">
                <span className="repo-stars">★ {formatCount(detail.stars)}</span>
                {detail.language && (
                  <span className="repo-lang">
                    <span className="lang-dot" style={{ backgroundColor: languageColor(detail.language) }} aria-hidden="true" />
                    {detail.language}
                  </span>
                )}
                <span>浏览 {formatCount(detail.views)}</span>
                <span>{formatTime(detail.published_at)}发布</span>
                <span className={`source-badge badge-${detail.source}`}>
                  {detail.source === 'submitted' ? '投稿' : '采集'}
                </span>
                <span className="repo-meta-actions">
                  <IconAction kind="like" on={liked} count={detail.likes} busy={busy === 'like'}
                    onClick={() => gate(() => void toggleLike())} />
                  <IconAction kind="favorite" on={faved} count={detail.favorites_count} busy={busy === 'fav'}
                    onClick={() => gate(() => void toggleFav())} />
                </span>
              </p>
              {detail.topics.length > 0 && (
                <p className="repo-topics">
                  {detail.topics.map((t) => (
                    <Capsule key={t} label={t}
                      bg={capsuleBg(languageColor(detail.language))}
                      fg={capsuleText(languageColor(detail.language))} />
                  ))}
                </p>
              )}
            </section>

            <Tabs items={TAB_ITEMS} active={tab} onChange={switchTab} panelId="repo-tab-panel" />

            <div className="repo-tab-body" key={tab} id="repo-tab-panel" role="tabpanel" aria-label={tab === 'files' ? '文件预览' : '仓库解读'}>
              {tab === 'files' && (
                <div className="repo-files">
                  <FileTree tree={tree} current={file?.path ?? null} onSelect={(p) => onSelectFile(p)} />
                  {treeError && (
                    <p className="file-fallback">
                      文件树加载失败，<a href={detail.github_url} target="_blank" rel="noreferrer">去 GitHub 查看 ↗</a>
                    </p>
                  )}
                  {!treeError && file && <CodeView file={file} githubUrl={detail.github_url} defaultBranch={detail.default_branch} />}
                  {!treeError && fileError && (
                    <p className="file-fallback">
                      文件预览失败，<a href={detail.github_url} target="_blank" rel="noreferrer">去 GitHub 查看 ↗</a>
                    </p>
                  )}
                </div>
              )}
              {tab === 'explain' && (
                <div className="explain-placeholder">
                  <img className="explain-img" src={explainPlaceholder} alt="" aria-hidden="true" />
                  <p className="explain-title">解读功能建设中</p>
                  <p className="explain-sub">AI 将把这个仓库讲给中文开发者听（第 2 块规划）</p>
                </div>
              )}
            </div>

            <ReadmeSection repoId={repoId} tree={tree} />

            <section className="repo-discussions">
              <h2 className="discussions-title">讨论</h2>
              {detail.discussions_open ? (
                <p className="discussions-body">评论区（giscus，后端就绪后接入）</p>
              ) : (
                <p className="discussions-closed">作者未开启讨论</p>
              )}
            </section>
          </div>

          <RepoRail repo={detail} />
        </div>

        <LoginModal open={loginOpen} onClose={() => setLoginOpen(false)} />
      </main>
    </>
  )
```

d) `{actionError && …}` 行挪进 `repo-head` 区块尾部（紧跟 topics 段落之后）：

```tsx
            {actionError && <p className="action-error" role="alert">{actionError}</p>}
```

e) 其余状态逻辑（loadAll/selectFile/gate/toggleLike 原样保留）不变。

- [ ] **Step 4: RepoPage.css 增加 Grid 规则**

追加：

```css
/* 2026-08-27 五项改进 §3.5：左滚右停双列 */
.repo-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 24px;
  align-items: start;
}
.repo-main { min-width: 0; }
.repo-meta { display: flex; flex-wrap: wrap; align-items: center; gap: 12px; }
.repo-meta-actions { margin-left: auto; display: flex; align-items: center; gap: 14px; }

@media (max-width: 1100px) {
  .repo-grid { grid-template-columns: 1fr; }
  /* 单列堆叠时右栏取消吸附（.repo-rail-sticky 的 static 化在 RepoRail.css 有对应窄屏规则时可省，此处双保险） */
  .repo-rail-sticky { position: static; }
}
```

同时删除旧 `.repo-head` 里与新 meta 冲突的绝对/浮动残留（若有 `display:flex` 固定排布的 `repo-head-actions` 规则一并移除——文字按钮已不存在）。

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run src/pages/RepoPage.test.tsx`
Expected: PASS（既有「切换文件」「解析占位」「tab 写 URL」「?file 深链」「不存在兜底」「点赞回滚」等全部仍绿）。

- [ ] **Step 6: 提交**

```bash
git add src/pages/RepoPage.tsx src/pages/RepoPage.css src/pages/RepoPage.test.tsx
git commit -m "feat(frontend): 仓库页 skillsmp 式双列布局（图标按钮/自述文件/右栏集成）"
```

---

### Task 11: 全量验证与截图目检清单

**Files:** 无新增/修改（验证任务）

**Interfaces:**
- Consumes: 前 10 个任务的全部产出。

- [ ] **Step 1: 类型检查与全量测试**

Run: `npm run typecheck`
Expected: 无错误退出。

Run: `npm run test`
Expected: 全部 PASS（含既有的 TopBar/Tabs/搜索/投稿等套件）。

- [ ] **Step 2: dev 截图目检（需求①–⑤逐项核对）**

Run: `npm run dev`（后台保持），按清单核对并把截图存入临时目录：

| 需求 | 打开地址 | 核对要点 |
|---|---|---|
| ① | `/user/alice` | 深蓝横幅盖住头像+文字区直至「我的仓库」一行的上方；白字清晰；签名编辑框有暖白底 |
| ② | 任一短内容页（如空态分支） | 页脚贴住视口底部 |
| ④ | 全站任意页 | 页面底呈明显杏色 `#F4E4C2`，卡片边界依旧清楚、分割线可见 |
| ⑤ | `/repo/1` | 心形空心描边 26px、金星空心 29px 顶缘对齐；点亮变 `#D84870`/`#E9A63C` 并弹动一次；计数正确 |
| ③ | `/repo/1` | 左列：名称/元信息+图标 → 文件预览 tab → 自述文件 README 渲染 → 讨论；右列：作者卡+跳转 GitHub↗+创作者入口 sticky 固定，下方同类推荐卡（AI 分类应见 llm-eval-kit）；窗口缩到 <1100px 右栏下移；缩窗测试 `/repo/11`（其他分类）显示「同分类暂无更多仓库」 |

目检发现问题回到对应任务修复（修完重跑该任务测试与本任务清单）。

- [ ] **Step 3: 确认 git 干净（仅本计划的文件被提交过）**

Run: `git status --porcelain`
Expected: 除用户自己的首页相关未提交改动外，不应出现本计划涉及的路径。

---

## Self-Review 结论（已完成）

- 规范覆盖：设计文档 §3.1→Task 3、§3.2→Task 1、§3.3→Task 2、§3.4→Task 4+10、§3.5→Task 5–10、工程验收 §4→Task 11。无缺口。
- 占位符扫描：无 TBD/TODO；所有代码块可直接落地。
- 类型一致性：`IconAction` props、`related` 签名、`favorites_count?: number`、`RelatedList items`、`RepoRail repo` 在定义任务与消费任务间逐一吻合；测试中 fixture 数值（id1 likes 45/fav 24、id2 fav 5→交互后 6）与 fixtures.ts 一致。
